# SPEC — gloggy

distilled 2026-04-21 from code + the since-removed v3 kit tree (`git log -- context/kits/`). this spec is single-source for drift checks. kit identifiers preserved verbatim (R#, T-###, F-###).

## §G goal

TUI for interactive JSONL log analysis. single binary. file or stdin. go + bubbletea.

## §C constraints

- go ≥1.26. macOS + linux only (fsnotify). windows out of scope.
- TUI only — bubbletea/lipgloss/termenv stack. UTF-8 assumed.
- single file input OR stdin. no multi-file, no remote, no compressed, no format-switch.
- TrueColor honoured via COLORTERM=truecolor|24bit (else termenv downsample).
- terminal floor 60x15 → "too small" message (as:R2 AC7).
- deps pinned: bubbletea v1.3.10, lipgloss v1.1.0, fsnotify v1.9.0, go-toml v2.3.0, atotto/clipboard v0.1.4.

## §I interfaces

### I.cli

- `gloggy <file>`            — load file
- `gloggy -f <file>`         — file + follow
- `gloggy [-f]` (piped stdin) — stdin + auto-follow (V31); `-f` redundant-accepted
- no file & no pipe → invalid, stderr err + exit 1 (as:R1)

### I.cfg

- path: `$XDG_CONFIG_HOME/gloggy/config.toml` (via `os.UserConfigDir`)
- created w/ defaults on first run. unknown keys preserved on write-back.
- schema (`internal/config.Config`):
  - `theme` ∈ {`tokyo-night`(def), `catppuccin-mocha`, `material-dark`, `solarized-dark`}
  - `compact_row.fields` def=[time,level,logger,msg]; `.sub_fields` def=[]
  - `hidden_fields` def=[]
  - `logger_depth` def=2
  - `detail_pane.height_ratio` def=0.30; `.width_ratio` def=0.30
  - `detail_pane.position` def=`auto` ∈ {`below`,`right`,`auto`}
  - `detail_pane.orientation_threshold_cols` def=100
  - `detail_pane.wrap_mode` def=`soft` (only mode shipped; `scroll`/`modal` future)
  - `scrolloff` def=5 — **top-level shared key**, NOT nested (cfg:R5)

### I.entry (`internal/logsource.Entry`)

`{LineNumber int, IsJSON bool, Time time.Time, Level, Msg, Logger, Thread string, Extra map[string]json.RawMessage, Raw []byte}`. RFC3339Nano. unparseable time → zero (ls:R5).

### I.msgs (bubbletea stream)

- `EntryBatchMsg{Entries []}` + `LoadProgressMsg{Count}` + `LoadDoneMsg` — load stream (drain via `LoadFileStreamMsg.Next()`)
- `TailMsg{Entries []}` + `TailStopMsg{Err}` — tail stream. batching per-source: fsnotify Write event (file, ls:R8) | 50ms timer-flush (stdin, V31).

### I.keys (canonical — full list in README + as:R5 help overlay)

- nav: j/k g/G Ctrl-d/u PgDn/PgUp Space/b (dp) Home/End (dp)
- el: e/E w/W m/M u/U l/h Tab Enter / n/N  (M = clear all marks, silent no-op on zero)
- dp: / n/N + - = | Esc
- fp (filter panel): j/k Space a e d Esc  (a=add blank prompt; e=edit highlighted in pre-filled prompt; `e` ≠ list `e`-ERROR — different domain ∴ V14-style domain-precedence ordering applies)
- global: f F y ? q T t

### I.themes (`internal/theme`)

tokens: LevelError/Warn/Info/Debug, Key/String/Number/Boolean/Null, Mark, Dim, SearchHighlight, CursorHighlight, HeaderBg, FocusBorder, DividerColor, UnfocusedBg, DragHandle, BaseBg. each ctor cites upstream source (`TokyoNightSource` etc).

### I.themesel (theme selector overlay — see V29)

- trigger: global `T` (V14-gated)
- form: full-screen modal overlay (per as:R5 help pattern)
- nav: ↑/↓ or k/j cycle highlight; Enter commits; Esc reverts
- shows: bundled themes only (`tokyo-night`, `catppuccin-mocha`, `material-dark`, `solarized-dark`); user-defined themes out of scope
- live preview: each highlight change → whole-TUI repaint in highlighted theme
- commit (Enter): writes `theme=<name>` to config.toml via existing write-back (V22-preserves unknown keys) + closes
- revert (Esc): restores pre-open theme + closes; no config write
- direct cycle: global `t` (V14-gated) — see V30
- cycle order: `tokyo-night → catppuccin-mocha → material-dark → solarized-dark → tokyo-night` (bundled declaration order)
- per press: advance + persist `theme=<name>` via config write-back (V22) + whole-TUI repaint via single `theme.GetTheme` path; no overlay, no preview window
- overlay-open precedence: `t` is overlay's domain (no parallel cycle) — single-source mutation

## §V invariants

- **V1** (ls:R8) tail batched per FS event: 1 `TailMsg` per drain, `len(Entries)` = lines made available. initial pre-watcher drain = 1 batch. per-line emission = kit violation (causes row-by-row scroll animation on `gloggy -f bigfile`).
- **V2** (el:R14) `AppendEntries(K≥1)` ⇒ EXACTLY one cursor-snap + one viewport-adjust + one selection-signal, regardless of K. symmetric across `TailMsg` + `EntryBatchMsg`.
- **V3** (el:R14) tail-follow pre-cond: `Cursor == Total-1` before append → auto-advance + scrolloff bottom-edge. else cursor/viewport UNCHANGED. `IsAtTail()` drives `[FOLLOW]` badge live per render.
- **V4** (el:R1) each compact row = exactly 1 terminal line. embedded `\n` in msg/raw flattened to space pre-render.
- **V5** (el:R6) list `View()` outputs EXACTLY `ViewportHeight` rows (padded w/ blanks if short). no buffer, no overflow — bubbletea `JoinVertical` does not tolerate extra rows.
- **V6** (cfg:R5) `scrolloff` is **top-level** TOML int. single source of truth for el:R12 + dp:R11. NOT splittable per-pane. negative/non-int clamped to 0 + warn. at use: clamped `[0, floor(VisibleRows/2)]`.
- **V7** (el:R3, dp:R2, as:R3, as:R10) NO hardcoded colors anywhere. every fg/bg/attribute resolves through active theme token. render output ANSI must contain theme's token value.
- **V8** (el:R10, dp:R10) layout math has **single owner** (as:R2). pane mouse-handlers must NOT re-derive terminal-Y→row or subtract borders a 2nd time. violations: 2-row-offset click bug (F-127); detail underfill/overflow (F-103/F-104).
- **V9** (el:R10 AC6) list click-row resolver MUST reject clicks when `contentTopY` unset — either panic-on-unset or "wired" flag returning no-row. zero-default → silent 2-row-offset regression (F-127).
- **V10** (dp:R11) detail-pane cursor-row bg is **contiguous** across col 0..content-width. must survive inner `\x1b[0m` SGR resets from per-token syntax highlighting — strip inner resets on cursor row OR cell-level bg paint after reflow. byte-concat of `Style.Render()` is kit violation.
- **V11** (dp:R9) soft-wrap preserves SGR state across wrap boundary. continuation line reopens same style. raw `ansi.HardwrapWc(preserveSGR=false)` forbidden.
- **V12** (dp:R1, as:R11) opening detail pane does NOT transfer focus — stays on list for live-preview. focus transfer only via Tab, mouse-click-in-pane, or `/` (as:R13).
- **V13** (as:R13) `/` routes by **current focus**, not pane-open state. list-focused → list search (el:R13); dp-focused → dp search (dp:R7); filter-panel-focused → literal char. never silent no-op.
- **V14** (as:R14) global single-key reservations (`q`, `Tab`, `?`, `Esc`) MUST NOT preempt active in-pane search input. `q`/`?` → query char, NOT `tea.Quit`/help-open. exceptions: `Tab` still dismisses search via focus-cycle; `Esc` routed through pane search first. **at-impl**: global/overlay interceptors (`HelpOverlayModel.Update` + any future pre-`handleKey` consumer) MUST gate on "no pane search in input mode" before consuming a key. top-of-`Update` unconditional consumption = kit violation (B5). tests asserting a reserved key (`q`/`?`) fires its global action while pane-search is in input mode = kit violation — flip the precondition (no active search) or assert the key becomes a query char.
- **V15** (as:R9) `y` never silent: marked-entries → notice w/ count; 0 marked → "no marked entries"; clipboard err → visible err notice. `//nolint:errcheck` on `CopyMarkedEntries` = kit violation.
- **V16** (as:R7, dp:R6) `height_ratio` + `width_ratio` preserved **independently** across orientation flips. below-mode uses `height_ratio` only for vertical; right-mode vertical height = full main slot, NOT `height_ratio × term_h` (content-loss bug).
- **V17** (as:R12) ratio clamp `[0.10, 0.80]`. per-key semantics (`NextDetailRatio`, `ratiokeys.go`): `+` grows focused pane by `RatioStep` (0.05); `-` shrinks focused pane by `RatioStep`; `=` resets to `RatioDefault` (0.30) regardless of focus; `|` cycles focused pane's share through presets {0.30, 0.50} — **off-preset ratio jumps to first preset (0.30)**, not wrap (B4 gap). at boundary = no-op: value unchanged AND config write-back skipped. `handleRatioKey` MUST guard `saveConfig()` on `newR != current` — unconditional save = kit violation (B3). detail closed → all 4 keys (`|+-=`) silent no-op.
- **V18** (as:R15) drag is focus-neutral + single-persist (one config write on Release, not per-Motion). bare Press+Release w/ no Motion → no config write. press-on-current-divider → current ratio unchanged (inverse math = exact inverse of forward `PaneHeight/Width`).
- **V19** (as:R15 drag-seam) drag-seam scope: right-mode = the 1-cell `│` divider glyph. below-mode = detail pane's TOP border row ONLY — list's bottom border is an adjacent row rendered in list's focus-state color, NOT shared (F-201).
- **V20** (cfg:R4) `DragHandle ≠ DividerColor` AND `DragHandle ≠ FocusBorder` in every bundled theme. `BaseBg ≠ UnfocusedBg` within each theme; `BaseBg` pairwise distinct across themes.
- **V21** (as:R7) on `position=auto`, every `WindowSizeMsg` crossing `orientation_threshold_cols` must refresh ALL pane-local orientation-dependent flags — both in resize handler AND in `relayout()`. miss → seam renders in pre-flip colors (F-200).
- **V22** (cfg:R3, cfg:R6) unknown TOML keys preserved on write-back. live writes preserve existing unrelated values.
- **V23** (ls:R8) follow ∈ {file, stdin}. `-f <file>` → file follow. piped stdin → auto-follow (no flag needed; `-f` redundant-accepted). bare `gloggy <file>` = load-no-follow. stdin-follow batching per V31.
- **V24** (ls:R7) loading never blocks UI. progress signals stream; UI renders partial results mid-load.
- **V25** (as:R9) V15 coverage MUST include live-buffer verification. unit tests asserting `m.View()` contains the notice string are necessary but NOT sufficient: (a) renderer-level issues (diff-renderer skips, line-replace width drift) could drop the row before it reaches the pty; (b) perceptual issues (notice style indistinguishable from keyhints) render bytes into the pty that humans cannot parse as feedback. every `y`-feedback path (copied-N, no-marks, clipboard-err) requires a tea.Program capture-renderer test OR a pty-driven integration test. a capture-renderer test catches (a); catching (b) also requires a tui-mcp / pty screenshot check that asserts the notice is visually distinct from the keyhints row (style, contrast, or bold). (gap revealed by B1: passing V15-aligned unit tests coexisted w/ a live TUI where the notice bytes reached the pty but blended into the Dim keyhints row.)
- **V26** (el:R1/R9) list `View()` prefix slot MUST be accounted for in compact-row width math. list prepends a 2-cell prefix (`"* "` mark / `"⌀ "` pin / `"↻ "` wrap-indicator) based on row state; the width passed to `RenderCompactRow` MUST be `m.width - 2` when a prefix is present, OR a dedicated 2-cell prefix column reserved at leftmost position of every row (padded empty when absent). naive `prefix + RenderCompactRow(m.width)` concatenation yields a `width+2`-cell row that soft-wraps to 2 terminal lines — violates V4 + cascades into V5 by emitting an extra row that JoinVertical steals from the adjacent header slot.
- **V27** (tests) `*_test.go` files MUST use `github.com/stretchr/testify` — `require` for fatal assertions, `assert` for non-fatal. stdlib `t.Errorf`/`t.Fatalf`/`t.Error`/`t.Fatal` forbidden in `*_test.go`. rationale: consistent failure messaging, diff-style equality output, less `if err != nil { t.Fatalf(...) }` boilerplate. at-use: `require.NoError(t, err)`, `require.Equal(t, want, got)`, `assert.Len(t, xs, 3)`. kit violation: bare `if got != want { t.Errorf(...) }` or `if err != nil { t.Fatal(err) }`. exception: `t.Helper`, `t.Skip*`, `t.Log*`, `t.Run`, `t.Cleanup`, `t.TempDir`, etc remain stdlib (not failure reporters).
- **V28** (rendering) full-screen replacement views (help overlay + any future overlay returning its own `View()`) MUST NOT use `\t` for alignment. bubbletea's line-diff renderer does not clear cells that `\t` skips over — they retain bytes from the previous frame, bleeding prior content into the overlay. lipgloss does not expand tabs either. at-impl: pad with spaces (column width = max key length across all domains + constant gap). tests MUST assert `View()` contains zero `\t` for any full-screen-replacement view. kit violation: `sb.WriteString("\t")` (or any raw `\t` emission) in a view builder.
- **V29** (I.themesel) theme selector overlay: trigger `T` MUST gate on V14 — active pane-search input mode → `T` becomes query char, NOT overlay-open. open → highlight = current theme. ↑/↓/k/j cycle; Enter commits + persists `theme=` via config write-back (V22 preserves unknown keys); Esc reverts to pre-open theme + no config write. live-preview: each highlight change repaints whole TUI in highlighted theme via single `theme.GetTheme` resolution path (no parallel palette code). closed overlay → zero mutation (no theme swap, no config mtime advance). overlay full-screen-replacement view → V28 applies (no `\t` padding). tests MUST cover: (a) `T` during pane-search input mode → query char (V14); (b) navigate → `View()` ANSI bytes carry highlighted theme tokens; (c) Enter → `config.toml` `theme` key updated + unrelated keys preserved; (d) Esc → pre-open theme restored + no config mtime advance.
- **V30** (I.themesel direct-cycle) direct theme cycle key `t` MUST gate on V14 — active pane-search input mode → `t` becomes query char, NOT cycle. each press advances through bundled themes in declaration order (`tokyo-night` → `catppuccin-mocha` → `material-dark` → `solarized-dark` → wrap), writes `theme=<name>` to config.toml via existing write-back (V22 preserves unknown keys), and repaints whole TUI via single `theme.GetTheme` resolution path (no parallel palette code). no overlay, no live preview — commit is immediate per press. theme selector overlay (V29) open → `t` consumed by overlay (no cycle, no config write); single-source mutation invariant. closed overlay + no pane-search → `t` cycles. tests MUST cover: (a) `t` during pane-search input mode → query char (V14); (b) sequential `t` presses from `theme=tokyo-night` advance `catppuccin-mocha` → `material-dark` → `solarized-dark` → wrap to `tokyo-night`; (c) each press updates `config.toml` `theme` key + preserves unrelated keys; (d) overlay (V29) open → `t` is overlay-domain no-op (no theme swap, no config mtime advance).
- **V31** (ls:R8 stdin) stdin-follow batching: timer-flush 50ms. coalesce parsed lines into 1 `TailMsg` per tick; `len(Entries)` = lines parsed since last tick. 0-entry tick → no emission. stdin EOF → `TailStopMsg{Err=nil}` (pipe close = normal); TUI stays interactive, `[FOLLOW]` badge drops on EOF (V3 `IsAtTail()` path unchanged post-stop). per-line emission = kit violation (row-by-row scroll-animation failure mode, same as V1 on files). stdin synchronous pre-TUI drain (pre-V31 behavior) removed — TUI opens immediately, entries stream via `TailMsg`. V2 invariance holds: `AppendEntries(K≥1)` ⇒ exactly 1 cursor-snap + 1 viewport-adjust + 1 selection-signal per `TailMsg`, symmetric w/ file-follow path.
- **V32** (as:R9 V15-pattern) global filter-toggle `F` MUST gate on V14 — active pane-search input mode → query char, NOT toggle. outside search: invokes `FilterSet.ToggleAll` — 1st press saves per-filter `Enabled` + disables all; 2nd press restores saved state (filters disabled pre-toggle stay disabled; filters added while globally-disabled restore to their Add-time `Enabled` per `filter.go:142`). emits `FilterChangedMsg` so filtered index recomputes via same V2 cursor-snap path as panel toggles. never silent (V15 pattern): 0 filters → "no filters" notice; on-toggle → "filters disabled"/"filters restored" notice. 0 filters = state-unchanged + notice, no `FilterChangedMsg`. kit violation: invoking ToggleAll silently, or skipping V14 gate (`F` preempts search). MANUAL-TOGGLE EXIT: while `globallyDisabled=true`, invoking `FilterSet.Enable(id)` | `FilterSet.Disable(id)` (panel Space, future API) ! clear `globallyDisabled` + `savedEnabled`. user's manual toggle exits the global-toggle state machine ∴ subsequent `F` starts a fresh 1st-press cycle from the post-Space state. kit violation: leaving `globallyDisabled`/`savedEnabled` stale after manual Enable/Disable — next `F` hits the "restore" branch + silently clobbers the user's Space change while re-enabling filters the user believed `F` had cleared. (gap revealed by B11: user repros "F doesn't always clear all filters" via tui-mcp after Space-toggle interleaved between two `F` presses; restore branch reinstates `savedEnabled` snapshot that predates the Space mutation.)
- **V33** (as:integration) every user-facing interaction documented in README §Features | SPEC §I ! wired end-to-end along BOTH axes:
  - UPDATE-AXIS: triggering emitter (key binding, mouse handler, etc.) ! have matching `case <Msg>:` | dedicated handler in `app.Update` that drives visible outcome.
  - VIEW-AXIS: every sub-model (panel, overlay, prompt, pane) whose `View()` carries user-visible state ! be composed into `Model.View()`'s render output — via `m.layout.Render(...)` args, a modal-replacement branch, | explicit overlay call site. sub-model `View()` that is constructed + key-routed + never rendered = kit violation.
  - isolated unit tests of sub-model `Update`/`View` necessary but ⊥ sufficient; ≥ 1 integration test per axis ! assert `m.View()` contains the rendered glyphs (| the msg-consuming state mutation) after the triggering input.
  - kit violation UPDATE-AXIS: README | spec claims UX + ships isolated test, emitter wired, consumer absent ∴ `app.Update` sees msg & discards (no `case`), user press → void. (gap revealed by B7: `detailpane.FieldClickMsg` emitted by pane mouse handler, `uifilter.PromptModel` unit-tested, both suites green, `go test ./...` passes, but `internal/ui/app/model.go` Update had no `case detailpane.FieldClickMsg` + never instantiated prompt ∴ documented click-field-to-filter UX never reached user.)
  - kit violation VIEW-AXIS: README | spec claims overlay + ships isolated View() test, `m.focus` flipped by key handler, sub-model constructed + key-routed, but `Model.View()` never invokes `m.<sub>.View()` ∴ keypress becomes silent focus-flip while live TUI shows unchanged frame. (gap revealed by B10: `uifilter.Model.View()` unit-tested, `m.filterPanel` constructed + fed keys via `case FocusFilterPanel`, `f` sets `FocusFilterPanel`, but `model.go:812-863` Model.View() composition omitted `m.filterPanel.View()` ∴ panel invisible in live TUI while `go test ./...` passed.)
- **V34** (tests) layout-math helper unit tests MUST assert expected Y | row values against a ground-truth reference (rendered-frame snapshot | independent layout arithmetic) — NOT against the impl's own formula. kit violation: test body computes `expected` by re-running the impl's arithmetic (∴ test passes whatever impl returns, incl bugs). round-trip integration tests compound this: helper A (`DetailPaneContentTopY`) feeds helper B (`ClickToPaneRow`) → forward + reverse mapping cancel formula errors inside the harness, silent agreement regardless of correctness. at-impl: (a) layout helper tests derive `expected` from HeaderHeight + EntryListHeight + border-counts independently, OR from a captured render (tui-mcp snapshot w/ known glyph Y); (b) integration tests that round-trip ≥ 2 helpers MUST include ≥ 1 ground-truth Y assertion read from actual rendered output. (gap revealed by B8: `TestLayout_DetailPaneContentTopY_Below` asserted 22 matching impl return 22; `TestLayout_ClickToPaneRow_FirstContentRow` passed (22 → paneLocalY 0) symmetrically; `TestModel_FilterPrompt_MouseClick_Enter_AddsFilter_V33` computed click Y via the same buggy helper. `go test ./...` green while live TUI resolved clicks off by 1 row — only tui-mcp HUMAN verify exposed the drift.) AMEND 2026-05-30: layout property-tests MUST also cover V41 idx round-trip identity + cursor/detailpane empty-edge contracts, `expected` derived from ground-truth (independent arithmetic | rendered snapshot), never impl re-run.
- **V35** (filter UX) `Filter{Field, Pattern, Syntax, Mode, Enabled}` w/ `Syntax ∈ {Literal, Glob, Regex}` explicit per filter — no auto-classify by metachar (current `containsMetaChar` dispatch in `match.go` removed). `Field == ""` sentinel = whole-line: `Match` resolves value as `string(entry.Raw)` (raw JSON bytes — keys + values + punctuation). `Field != ""` = field-scoped, resolves via existing `entryFieldValue` unchanged. Glob → regex: escape RE2 metachars first, then `*` → `.*`, `?` → `.`; NO auto-anchoring (substring-feeling preserved across Literal/Glob/Regex). Validation MUST happen at Add/Edit commit (new `FilterSet.Validate(Filter) error`): empty Pattern → reject + V15-pattern notice "pattern required"; bad Regex → reject + notice "bad regex: <err>"; Glob translator is deterministic (no error path). Runtime `Match` swallow path (`ok, _ := Match`) MUST go silent only on field-absent — pattern errors caught upstream at Validate ∴ never reach `Match`. Kit violation: silent regex compile failure at `Match` time (bad regex = filter that silently matches nothing, indistinguishable from a working filter w/ zero hits).
  PANEL ROW FORMAT (Direction A):
  - layout: `[x|space] +|−  <field>|«line»:<pattern> [·glob|·regex]`
  - `+` (include) themed via `LevelInfo` token; `−` (exclude) via `LevelError` token — V7-compliant (no hardcoded colors).
  - `«line»` placeholder marks whole-line filter; field name appears otherwise.
  - Syntax suffix dim-styled, OMITTED when `Literal` (common-case noise reduction).
  - Disabled row (`[ ]`) renders entire line in `Dim` token.
  CURSOR VISIBILITY:
  - PANEL mode (navigating filters): highlighted row carries `> ` prefix + theme `CursorHighlight` bg over full row width (V10 contiguous-bg invariant applies).
  - PROMPT mode (Add/Edit form): focused input row carries `> ` prefix + theme `CursorHighlight` bg over full row width; unfocused rows render w/o prefix + base bg.
    - Text-input rows (Field, Pattern): focused row appends `█` caret immediately after input text; empty input → caret right after label padding.
    - Enum-cycle rows (Syntax, Mode): active value bracketed `[<active>]` on EVERY row (focused or not — user always sees current selection); cycle hint `◂ ... ▸` rendered ONLY on focused row; footer key-hint row adapts to advertise `←/→ cycle` when focus is on enum row.
  - PROMPT-active-inside-PANEL: prompt overlay fully replaces panel View (no dual-cursor — single visible cursor at all times). Esc returns to panel, last-navigated panel row restored.
  - Kit violation: focus indicator by color alone (low-contrast themes risk silent invisibility); always pair `> ` prefix + bg highlight (defense-in-depth, same lesson as V25 perceptual-distinctness).
  ADD/EDIT FLOW: unified multi-row prompt (Field, Pattern, Syntax, Mode). Tab/Shift+Tab cycles row focus (4-way wrap); text input on Field+Pattern rows; ←/→ cycles enum rows; Enter validates then commits; Esc cancels. Pre-fill: pane-click → Field+Pattern set + Syntax=Literal + Mode=Include; panel `a` → blank (Field=`""`, Pattern=`""`, Syntax=Literal, Mode=Include); panel `e` → current filter values + id. Cursor lands on PATTERN row in ALL three invocations (most-common edit target). Empty Field at commit ⇒ whole-line filter; non-empty Field ⇒ field-scoped. CONVERSION is side-effect of editing Field row (no separate convert command). Edit reuses filter id ∴ preserves position in `FilterSet` + Enabled state (new `FilterSet.Update(id, Filter) bool`). V32 global-toggle state-machine unaffected by Edit: Edit does NOT mutate `Enabled` directly ∴ subsequent `F`-restore replays unchanged ID→bool snapshot — no clobber. Mode change in-panel is route-via-`e` only (no dedicated `m` key — minimum key surface). README §Filter Engine + help overlay `DomainFilterPanel` MUST list panel keymap (j/k Space a e d Esc) + bullet on whole-line + wildcard syntax. V14 applies: prompt active intercepts ALL KeyMsg ∴ reserved-globals (`q`/`?`/`T`/`F`) become literal chars in Pattern field while focused (same policy as existing pane-search input mode). V33 axes apply: VIEW-AXIS integration tests assert `m.View()` carries V35 row format glyphs + cursor markers (`> ` prefix, `█` caret, `[...]` bracket, `◂ ▸` cycle hint) after add/edit/convert + focus changes; UPDATE-AXIS asserts msg routing for new `a`/`e` keys + edit-commit + reject messages.
  AMEND 2026-05-30 (validation tightening): `FilterSet.Validate` MUST reject `strings.TrimSpace(Pattern) == ""` w/ "pattern required" — current impl tests `Pattern == ""` ONLY ∴ whitespace-only Pattern passes + builds a filter that matches/excludes ~everything (B36). prompt Edit-commit MUST check `FilterSet.Update(id, f) bool` return — `false` (id removed between OpenEdit + Submit) ! emit `FilterRejectedMsg{"filter not found"}` + keep prompt open, NEVER silent close (B37). prompt input buffer (Field + Pattern text rows) rune-consistent per V36 — insert + backspace via `[]rune` round-trip, no rune-count-vs-byte-append mix (B15). kit violation: whitespace-Pattern accepted, `Update` return discarded, byte-append into multibyte buffer.

- **V36** (render-width) visible string widths via `lipgloss.Width` (terminal cells), NOT `len()` (bytes). every runtime truncation / prefix-pad / ellipsis / caret-placement = cell-width math. text-input edit buffers (insert + backspace) rune-consistent: single `[]rune` round-trip, no rune-count-vs-byte-append mix. UTF-8 input (CJK/emoji) MUST NOT corrupt buffer | over-truncate msg. at-impl: `entrylist/row.go` `visiblePrefixLen` (`len(AbbreviateLogger(...))`) + msg-fit (`len(msg) > remaining`) + raw-text `raw[:width]`; `ui/filter/prompt.go` insert/backspace. at-test: render helpers + prompt buffer w/ CJK+emoji fixture; assert `lipgloss.Width(out) == expectedCells`. kit violation: `len(s)` as width, `s[:n]` byte-slice over multibyte, `string(runes)+byteStr` concat. (gap: B12-B15.)
- **V37** (ls:R8 tail-survive) tail survives log-rotate + truncate-in-place + transient IO-err. (a) per-drain `bufio.Reader` MUST seek to last file offset before read | use one persistent reader — naked recreate caches bytes internally ∴ re-read | skip; (b) file size shrink between drains ⇒ reset `pending` partial-line buffer + reopen by path; (c) drain IO err ⇒ `TailErrMsg{Err, Retryable}` + exp-backoff reopen on Retryable (cap 5s), `TailStopMsg{Err}` only on non-recoverable; (d) loader `os.Open` err ⇒ `LoadErrMsg`, NOT silent `LoadDoneMsg(0)`. at-test: rotation suite — fixture renames file mid-stream + truncates + chmod-700 transient; assert zero line-loss + `[FOLLOW]` survives. kit violation: close tail on first IO err, swallow open-err as empty-load, pending kept across truncation. (gap: B16-B19.)
- **V38** (cfg:R3 atomic-write) config writes atomic: marshal → tempfile `<dir>/.cfgtmp-<pid>` → `f.Sync()` → `os.Rename(temp, path)`. crash | power-loss mid-write ⇒ file = last-good-state | absent, NEVER partial. underpins V22 (unknown-key preservation requires intact file). orphan `.cfgtmp-*` cleaned best-effort on Load. at-test: kill-process-mid-Save fixture (SIGKILL between marshal + rename, env-gated) asserts post-mortem file pre-write | absent. kit violation: `os.WriteFile(path, data, mode)` direct (truncate-in-place). (gap: B20.)
- **V39** (cli:R1 input-precond) ParseArgs input-source exhaustive: (a) `os.Stdin.Stat()` err surfaced — `stdinStat,_ := …` then `.Mode()` = nil-deref panic on err = kit violation; (b) piped-stdin + positional file arg = conflict ⇒ stderr msg + nonzero exit, NEVER silent file-precedence over stdin; (c) `-f` no-pipe no-file = invalid. valid modes: {tty+file} | {pipe-stdin (+ redundant `-f`)} | {tty + `-f` + file}. at-test: ParseArgs table — Stat-err, stdin+file conflict, `-f` redundancy w/ pipe, `-f` w/o pipe-no-file. (gap: B21-B22.)
- **V40** (filter ctor) `NewFilterSet` initializes ALL internal maps (`savedEnabled` + future). every public mutation (Add/Remove/Enable/Disable/ToggleAll/Update) safe from a fresh ctor regardless of call order — NO implicit "ToggleAll-ran-first" precondition. currently `Add` writes `savedEnabled[id]` under `globallyDisabled` while `NewFilterSet` returns `&FilterSet{}` (nil map) + map inits only in `ToggleAll` ∴ latent nil-panic if a future path flips the flag elsewhere. at-test: shotgun — each public method called alone from `NewFilterSet`, no panic. kit violation: nil-map write reachable via method-ordering. (gap: B24.)
- **V41** (layout idx-roundtrip) `visibleIndexForEntry` ∘ `entryIndexForVisible` = identity across ALL pin states {none, pin-in-filter, pin-out-of-filter, pin-at-edge, pin == cursor}. cursor contracts: empty filtered list ⇒ `CursorPosition() == 0` (NOT 1); empty entry list ⇒ idx-helpers return present=false form. detailpane empty content ⇒ EXACTLY `h` terminal ROWS, emitted as `strings.Repeat("\n", h-1)` (h rows ⇔ h-1 newlines; emitting `h` newlines = h+1 rows = WRONG). row-count assert = `strings.Count(out,"\n")+1`, cross-checked vs full/short-content branch. prior "(not `h-1`)" phrasing miscounted newlines as rows ∴ B27 false-positive (B46). `ScrollToLine(idx)` MUST NOT temp-mutate `s.height` mid-call — viewport threaded via param ∴ `followCursor` sees final height. `expected` per V34 (independent arithmetic | rendered snapshot, never impl re-run). kit violation: blank-fill via `h` newlines (h+1 rows), temp-height mutation, asym idx round-trip. (gap: B28 real; B25/B26/B27 false-positive — B46.)
- **V42** (tea-purity + focus-sync) `View()` is pure — read-only on `m` + sub-models. mutations (`m.list.Focused`/`m.list.Alone`, sub-model state) happen in `Update()` ONLY — bubbletea calls `View()` > 1×/frame ∴ compose-time write = stale-state bug. focus invariant: `m.focus` ALWAYS references a VISIBLE pane; any pane-close (auto-close, drag-collapse, ratio-min, explicit) syncs focus to a visible peer in the SAME Update step. pane re-sync paths (`appendToList`) preserve active in-pane state (pane-search query + scroll) — payload refresh ⊥ destroy interaction state. at-test: View-determinism (call 2×, byte-equal); post-close key routes to visible pane; pane-search survives append. kit violation: `View()` mutates model, focus left on hidden pane post-close, re-sync clobbers `m.paneSearch`. (gap: B29-B31.)
- **V43** (err-surface, broadens V15) every IO + persist err path surfaces via UI notice OR returns to caller. silent drop outside `*_test.go` MUST carry `//nolint:errcheck <reason>`. MUST-notice paths: `config.Save`, `logsource.Load` + scanner, `tail`/stdin IO err (V37), ratio-key no-op (V17 silent-noop — emit user signal). V15 (`y`-never-silent) is the per-feature instance; V43 generalizes the rule. at-test: inject err per path ⇒ notice-text emitted | parent returns err. kit violation: `_ = config.Save(...)` unannotated, scanner-err logged while UI shows "done", `readerDone` err discarded on ctx-race. (gap: B32-B35.)
  AMEND 2026-05-31 (live-buffer, inherits V25): every NEW user-facing notice path V43 adds (`config.Save` err, ratio-key no-op signal, filter `pattern required`, filter `filter not found`) carries V25's full perceptual-distinctness obligation — NOT just "notice-text emitted". unit `m.View()`-contains-string is necessary ⊥ sufficient: (a) capture-renderer | tea.Program test asserts the notice byte reaches the frame (catches diff-renderer drop); (b) pty/tui-mcp screenshot asserts the notice cells are visually distinct from the Dim keyhints row (differing SGR: Bold | non-Dim fg) — same B1 blend failure mode. kit violation: new notice rendered via `Foreground(th.Dim)` (indistinguishable from keyhints), | shipped w/ unit-only `View()`-string coverage + no live-buffer/screenshot check. (gap: B1 lesson generalized to all V43 notices.)

- **V44** (backprop discipline) every §B entry MUST be reproduced before its fix task lands — a RED test | live repro that goes green w/ the fix. a "fix" that only adds an already-passing test = unverified finding: code may be correct + review wrong. row/height contracts: terminal ROWS counted as `Count(s,"\n")+1`, distinguished from `\n` separators (N rows = N-1 newlines); ground-truth per V34 (independent arithmetic | rendered snapshot, never impl re-run). kit violation: §B row whose fix commit shows no prior failing state. (gap: B46 — review-sweep 2026-05-30 backprop'd B25/B26/B27 as code bugs w/o repro; T42 verification found all 3 already-correct, only B28 reproduced.)

## §T tasks

| id | status | desc | cites |
|----|--------|------|-------|
| T1 | x | human tui-mcp sign-off across all 3 themes × {80x24, 140x35} × {below, right} — README notes "not yet human tested" | V7,V10,V19,V20 |
| T2 | x | human verify tail-follow no-scroll-animation on `logs/big.log` w/ `gloggy -f` | V1,V2 |
| T3 | x | human verify mouse click-row resolver across orientations × focus states on `logs/small.log` | V8,V9 |
| T4 | x | human verify clipboard `y` notices (copied/empty/err) | V15 |
| T5 | x | human verify drag visuals (below+right) via tui-mcp screenshot + V18 bare-Press+Release no-config-write via `send_mouse`; motion-driven path covered by unit `TestModel_T156_*`/`_T164_*` — tui-mcp `send_mouse` exposes no Motion action, raw SGR motion via `send_text` not decoded | V17,V18,V19 |
| T6 | x | guard `saveConfig()` in `handleRatioKey` on `newR != current`; add regression test for no-mtime-advance at ratio boundary across `-`/`+`/`=`/`\|` | V17 |
| T7 | x | fix B1: diagnose + repair `y`-notice drop (keyhints line-replace vs bubbletea diff-renderer); add tea.Program capture-renderer OR pty-driven test for copied-N / no-marks / clipboard-err paths per V25 | V15,V25 |
| T8 | x | automate V25 class-(b) coverage — tui-mcp / pty-driven golden-frame or contrast-check test that launches gloggy, presses `y` on no-marks, reads the bottom row, and asserts the notice cells are visually distinct from the keyhints row (e.g. differing SGR style: Bold or different fg). repeat for all 3 themes × all 3 y-feedback paths (copied-N / no-marks / clipboard-err). | V15,V25 |
| T9 | x | impl `M` clear-all-marks in entrylist: add `MarkSet.Clear()`, wire `case "M"` in `list.go` (drops pin + resets mark-nav state, per u/U pattern), unit test empties MarkSet + verifies zero-count after Clear. 0-marks → silent no-op, no confirm. update help overlay (as:R5) + README keymap. | I.keys |
| T10 | x | fix B2/V26: list `View()` overflows `m.width` by 2 cells when a prefix glyph is present (`* ` mark / `⌀ ` pin / `↻ ` wrap). `list.go:740/743/745` concat `prefix + RenderCompactRow*(m.width)` → row = `m.width + 2` cells, soft-wraps to 2 terminal lines, cascades into V5 by displacing the header row from Y=0. impl option A: reserve a dedicated 2-cell prefix column inside `RenderCompactRow` + `RenderCompactRowWithBg` padding math (prefix glyph at cells [0:2], content at [2:width], padded empty when no prefix — single contract, no call-site branching). option B: pass `m.width - 2` to the renderer at the three call sites when `prefix != ""`. prefer A. add regression test: mark an entry whose msg fills the width, assert `m.View()` emits exactly `ViewportHeight` `\n`-separated rows + header visible at Y=0 (V4+V5+V26 guard). | V4,V5,V26 |
| T11 | x | migrate all `*_test.go` to testify: add `github.com/stretchr/testify` to `go.mod`; replace ~1253 `t.Errorf`/`t.Fatalf`/`t.Error`/`t.Fatal` across 58 files with `require.*`/`assert.*`. package-by-package commits for reviewability; `go test ./...` green after each package. | V27 |
| T12 | x | shrink + split `internal/ui/app/model_test.go` (~2444 LOC post-T11, was 2787 pre-testify) into topic-focused files (focus transitions, ratio keys, divider drag, search, resize/lifecycle, misc). **two-phase**: (1) simplification sweep — dedupe setup/fixtures via shared helpers, table-drive related one-offs w/ `t.Run`, drop redundant coverage (V-cited paths ! intact); (2) pure-move relocation of remaining tests into topic files (no func renames, no logic edits). one commit per phase-group so blame + coverage regressions stay reviewable. `go test ./internal/ui/app/...` green after each commit. do AFTER T11 lands. | V27 |
| T13 | x | audit `internal/ui/detailpane/model_test.go` (~664 LOC) — siblings (scroll/search/wrap/visibility/render) already topic-owned, so `model_test.go` holds Init/Update/View + focus/size integration. method: simplification sweep first (dedupe + table-drive per T12 phase 1), then split only if >~400 LOC remain. prefer fewer files + denser tables over aggressive splitting. `go test ./internal/ui/detailpane/...` green each commit. | V27 |
| T14 | x | audit `internal/ui/entrylist/list_test.go` (~624 LOC) — siblings (cursor/scroll/row/marks/search/leveljump) already topic-owned; remainder ! View + Update integration. same method as T13: simplification sweep first, split only if still oversized. `go test ./internal/ui/entrylist/...` green each commit. | V27 |
| T15 | x | fix B5: gate `HelpOverlayModel.Update` on pane-search-input-mode so `?` is consumed as a query char when list or detail search is active in input mode. impl option A: invert ordering — move `m.help.Update(msg)` *after* `handleKey` routes the key to the focused pane's search (cleanest — no predicate plumbing). option B: thread a "search-active" predicate through `Update` and short-circuit `m.help.Update` when true. prefer A. flip `TestModel_HelpOverlay_PreservesListSearchState` to assert `?` becomes a query char during active list search (query = `"abc?"`); add a new test covering `?` opens help when no pane-search in input mode. repeat both for detail-pane search. `go test ./internal/ui/app/... ./internal/ui/appshell/...` green. | V14 |
| T16 | x | fix B6: replace `\t` in `HelpOverlayModel.View()` with fixed-column space padding. impl: compute max key-string width across all domains (use `lipgloss.Width` for correct East-Asian / arrow-glyph widths) + a 2-space gap, then pad each key row to that column before writing the description. add unit test asserting `View()` contains no `\t` when open. optional tui-mcp regression: launch gloggy on `logs/tiny.log`, press `?`, `snapshot`, assert no background digits/words leak between key and desc. `go test ./internal/ui/appshell/...` green. | V28 |
| T17 | x | impl theme selector overlay per I.themesel: new `internal/ui/appshell/themeselector.go` (model + Update + View per `HelpOverlayModel` pattern); wire global `T` AFTER pane-search routing in `Model.Update` (V14-safe — same ordering lesson as T15/B5); threading: overlay holds pre-open theme + active highlight; highlight change → swap-theme msg to app model for whole-TUI repaint; Enter → existing config write-back + close; Esc → restore pre-open theme + close. update help overlay (as:R5) + README keymap. tests per V29 (a)-(d). | V14,V22,V28,V29 |
| T18 | x | [HUMAN via tui-mcp] verify theme selector live-preview + persist + revert across all 4 themes × {below, right} orientations on `logs/small.log`: launch gloggy, press `T`, navigate ↑/↓ asserting visual repaint, Enter to commit, kill+relaunch to verify persistence, then press `T` + Esc to verify revert + no config mtime advance. | V29 |
| T19 | x | impl direct theme-cycle key `t` per V30: extend `Model.Update` global key handling AFTER pane-search routing (V14-safe — same ordering lesson as T15/T17); add cycle helper (next-in-bundled-order) in `internal/theme` or `internal/ui/appshell`; on press → advance + config write-back (V22) + theme swap msg → whole-TUI repaint via single `theme.GetTheme` path; gate when theme selector overlay (V29) is open (overlay's domain). update help overlay (as:R5) + README keymap. tests per V30 (a)-(d). | V14,V22,V30 |
| T20 | x | [HUMAN via tui-mcp] verify direct theme cycle `t` across all 4 themes × {below, right} on `logs/small.log`: launch gloggy, press `t` repeatedly asserting visual repaint advances tokyo-night → catppuccin-mocha → material-dark → solarized-dark → wrap; kill+relaunch to verify last-pressed theme persists; press `T` (overlay) then `t` to confirm overlay consumes `t` (no parallel cycle). | V30 |
| T21 | x | impl `SolarizedDark` theme in `internal/theme/` (new ctor citing upstream — Ethan Schoonover solarized). register as 4th bundled theme in declaration order AFTER `MaterialDark` — cycle helper + overlay entry list inherit from the bundled slice, no code branch. update §C theme enum validator + help overlay (as:R5) + README keymap if theme names enumerated there. tests: V7 ANSI render contains SolarizedDark token; V20 `DragHandle ≠ DividerColor`, `DragHandle ≠ FocusBorder`, `BaseBg ≠ UnfocusedBg`, pairwise `BaseBg` distinct across all 4 themes; V29 overlay `View()` lists 4 entries incl. solarized-dark; V30 test (b) extended — cycle reaches `solarized-dark` before wrap. | V7,V20,V29,V30 |
| T22 | x | [HUMAN via tui-mcp] verify `solarized-dark` on `logs/small.log` across {80x24, 140x35} × {below, right}: set `theme=solarized-dark` in config.toml, launch gloggy, screenshot each orientation × size, spot-check token colors against upstream Solarized Dark palette (base03 bg, base0 fg, yellow/orange/red/magenta/violet/blue/cyan/green accents); then from `theme=tokyo-night`, press `t` × 3 → assert repaint advances catppuccin-mocha → material-dark → solarized-dark (V30 declaration order); press `T` → assert overlay lists 4 entries incl. solarized-dark. | V7,V20,V29,V30 |
| T23 | x | impl stdin-follow: remove synchronous pre-TUI stdin drain; new `logsource` stdin-tail path w/ 50ms timer-flush batcher → `TailMsg` (V31); EOF → `TailStopMsg{Err=nil}` + `[FOLLOW]` badge drops; `-f` redundant-accepted when piped stdin (no error); `gloggy -f` no-pipe no-file → invalid arg error (as:R1). unit tests: batcher coalesces burst of 1000 lines into ≤ (burst_ms/50)+1 `TailMsg`s; EOF path fires `TailStopMsg{Err=nil}` + TUI stays interactive; V2 invariance (1 cursor-snap per `TailMsg`) on stdin path mirrors file path. | V2,V3,V23,V31,I.cli |
| T24 | x | [HUMAN via tui-mcp] verify stdin-follow: launch `tail -f logs/small.log \| gloggy` via tui-mcp; observe entries stream w/o row-by-row scroll animation (V31 batching); `[FOLLOW]` badge live (V3); append lines upstream (`echo ... >> logs/small.log`) → batched append; close upstream (kill `tail`) → `[FOLLOW]` badge drops + TUI stays interactive; spot-check j/k nav + `/` search work post-EOF. | V3,V23,V31 |
| T25 | x | add unit test in `cmd/gloggy/main_test.go` for ParseArgs piped-stdin cases: swap `os.Stdin` w/ `os.Pipe()` reader (non-tty fd) → assert `FromStdin=true + FollowMode=true` (V23); repeat w/ `-f` flag → same result (`-f` redundant-accepted). restore `os.Stdin` via `defer`. | V23,I.cli |
| T26 | x | wire global `F` to FilterSet.ToggleAll: new case in Model.Update global key handling AFTER pane-search routing (V14-safe — same ordering lesson as T15/T17/T19); emits FilterChangedMsg; emits V15-pattern notice via keyhints notice path (mirror copied-N style). update help overlay (as:R5) + README keymap + README §Filter Engine bullet. tests per V32 (a)-(d). | V14,V15,V32 |
| T27 | x | [HUMAN via tui-mcp] verify `F` global filter-toggle on `logs/tiny.log` × {tokyo-night, catppuccin-mocha} (V15 contrast): launch gloggy, add 2+ filters via pane field-click prompt (per T28 wiring) — some matching, some not, close pane; press `F` → assert "filters disabled" notice visually distinct from Dim keyhints + all rows reappear (full set visible); press `F` again → assert "filters restored" notice + filtered state returns; kill + relaunch w/ no filters, press `F` → assert "no filters" notice + empty FilterSet unchanged. | V14,V15,V32 |
| T28 | x | wire filter-add e2e per README:58: add `filterPrompt uifilter.PromptModel` field to `Model` (`internal/ui/app/model.go`); new `case detailpane.FieldClickMsg:` in `Update` opens prompt w/ pre-filled field+pattern; prompt intercepts keys while active (early-return pattern per `help`/`themesel`, V14-safe — pane-search input mode ⊥ preempted); Enter → existing `FilterConfirmedMsg` → `refilter`; Esc → close, no mutation; `View()` overlays prompt row when active. update help overlay (as:R5) — new `filter-prompt` domain entries (Enter/Esc/Tab) OR extend DomainDetailPane. update README:58 if wording changes post-wire. integration test ! drive: pane open → mouse click on field line (e.g. `level:INFO`) → `FieldClickMsg` routed → prompt active w/ pre-fill → Enter → `FilterSet.GetAll()` len = 1 & `refilter()` applied (`cachedVisibleCount` ≠ total). further unit tests: Esc cancels + FilterSet empty; prompt active intercepts `/`/`q`/`?`/`F` as literal (V14-safe); V33 integration. post-T28, T27 replayable. | V13,V14,V15,V33,I.keys |
| T29 | x | fix B9: refactor `findFieldLineY` in `internal/ui/app/model_filter_prompt_test.go` (:114-125) to resolve the target field row's terminal Y by scanning `m.View()` (split on `\n`, locate first line containing `"<field>":`, return its index). Do NOT call `m.layout.Layout().DetailPaneContentTopY()` from the test — that is the helper under test; V34(b) prohibits the round-trip cancellation (B8 mode). App `Update` still calls `ClickToPaneRow(msg.Y)` internally ∴ the test now exercises `DetailPaneContentTopY → ClickToPaneRow` via genuine rendered-terminal-Y ↔ pane-row mapping rather than cancelling through the helper. Keep existing Enter/Esc assertions intact. `go test ./internal/ui/app/...` green. | V33,V34 |
| T30 | x | fix B10: render `m.filterPanel.View()` as a full-screen overlay in `Model.View()` (`internal/ui/app/model.go:812-863`) when `m.focus == appshell.FocusFilterPanel`. Follow `HelpOverlayModel`/`ThemeSelectorModel` pattern — return overlay View as the whole frame, `Esc` closes via existing branch at `model.go:562-565` (returns focus → `FocusEntryList`). Overlay body: current `uifilter.Model.View()` already formats filter list | `(no filters)`; wrap it with title + keyhints footer (`Space=toggle, d=delete, Esc=close`) + V28 space-padding (no `\t`). Empty-state copy update: `(no filters — open an entry, click a field in the pane to add one)` so the add-filter flow is discoverable from the panel. integration tests per amended V33 VIEW-AXIS: (a) seed 0 filters, press `f` → `m.View()` contains `(no filters` prefix; (b) seed 2 filters, press `f` → `m.View()` contains both `field:pattern` rows + `[x]`/`[ ]` glyphs; (c) press `Esc` from open panel → `m.View()` no longer contains panel glyphs (frame restored to pre-open list view); (d) press `f` while pane-search is in input mode → `f` is literal, panel does NOT open (V14). update help overlay (as:R5) `DomainFilterPanel` entries post-impl if the keyhints footer reveals any missing binding. `go test ./...` green. | V14,V28,V33,I.keys |
| T31 | x | fix B11: in `internal/filter/filter.go` `Enable(id)` + `Disable(id)`, when `fs.globallyDisabled == true`, clear `globallyDisabled = false` + `savedEnabled = nil` after mutating `f.Enabled`. Manual toggle exits the global-toggle state machine ∴ the next `F` press starts a fresh 1st-press cycle (save current + disable all). Unit tests: (a) `globallyDisabled=true` + `Enable(id)` → `globallyDisabled=false` + `savedEnabled=nil` + `f.Enabled=true`; (b) same shape for `Disable(id)`; (c) `Add()` behavior during `globallyDisabled` unchanged (still saves + disables new filter per `filter.go:52-54`). Integration test in `internal/ui/app/`: seed 2 filters both enabled → `F` → `Space` on one → `F` → assert both `Enabled=false` (fresh 1st-press, NOT stale restore) + `IsGloballyDisabled()=true`; `F` again → assert state matches post-Space snapshot. `go test ./...` green. | V32,V33 |
| T32 | x | extend `internal/filter/`: add `Syntax Syntax` field to `Filter` (zero-value=`Literal`); new `internal/filter/glob.go` w/ `globToRegexp(pattern string) (*regexp.Regexp, error)` — escape RE2 metachars first, then substitute `*` → `.*` + `?` → `.`, cache compiled (parallel to existing `regexCache` in `match.go`); rewrite `matchPattern` to dispatch on `f.Syntax` (Literal=`strings.Contains`, Glob=cached glob regex, Regex=existing cached RE2 path) — drop `containsMetaChar` auto-classify; add `Match` whole-line branch (`Field=="" → string(entry.Raw)`); new `FilterSet.Validate(Filter) error` (empty pattern + bad regex compile); new `FilterSet.Update(id int, f Filter) bool` (in-place mutate by id, preserves slice position + `Enabled` state, returns false if id absent). Unit tests: glob translation table (`*`, `?`, `**`, `?*`, empty, mixed w/ literal chars + escaped RE2 metachars like `.`/`(`/`[`); whole-line vs field-scoped `Match`; `Validate` rejects empty + bad regex (each w/ specific error msg); `Update` preserves slice position + does NOT alter `Enabled`; `Match` never observes a compile-error path post-Validate. `go test ./...` green. | V15,V35 |
| T33 | x | extend `internal/ui/filter/prompt.go` to multi-row form: rows {Field, Pattern, Syntax, Mode} w/ focus index; Tab/Shift+Tab cycles row focus (4-way wrap); text input on Field+Pattern; ←/→ cycles enum rows; Enter calls `FilterSet.Validate` → err → emit new `FilterRejectedMsg{Reason string}` + keep prompt open + display notice via keyhints; OK → Add (when `editID == nil`) or `Update` by id (when `editID != nil`); emit `FilterConfirmedMsg{FilterID, FilterSet, IsNew bool}`; Esc → `FilterCancelledMsg` unchanged. Pre-fill API: `OpenBlank()`, `OpenFromPaneClick(field, pattern)`, `OpenEdit(filter, id)`; cursor lands on Pattern row in all three. View per V35 layout w/ V28 space-padding (zero `\t`) + cursor visibility section (`> ` prefix + `CursorHighlight` bg on focused row; `█` caret on focused text row; `[...]` bracket on every enum row + `◂ ▸` cycle hint ONLY on focused enum row; footer key-hint adapts per focused row type). Unit tests per V33: Tab cycles focus (assert `View()` `> ` prefix moves between rows); ←/→ cycles enum (assert bracketed value advances); Enter→Validate→reject keeps prompt open + carries notice; Enter→OK emits `IsNew=true` on blank vs `IsNew=false` on edit; `OpenFromPaneClick` keeps existing pane-click behavior intact (regression). | V14,V15,V28,V33,V35 |
| T34 | x | wire `a` + `e` in `internal/ui/filter/panel.go`: `a` → `m.prompt.OpenBlank()`, `e` → on highlighted filter `m.prompt.OpenEdit(filters[cursor], ids[cursor])`; new helper `formatRow(filter Filter, cursor bool, th theme.Theme) string` implementing V35 Direction A row format (`+`/`−` themed via `LevelInfo`/`LevelError`, `«line»` placeholder when `Field==""`, `·glob`/`·regex` dim suffix omitted when `Literal`, whole-row `Dim` token when `!Enabled`, `> ` prefix + `CursorHighlight` bg when cursor row per V10); panel `View()` uses `formatRow` per filter; `Model.renderFilterPanelOverlay` keyhints footer updated to `a add · e edit · d delete · Space toggle · Esc close`; empty-state copy → `(no filters — press 'a' to add, or click a field in the detail pane)`; route `FilterConfirmedMsg{IsNew=false}` through `FilterSet.Update`. Integration tests per V33: seed 0 filters → `f` `a` "DrawState" Enter → `m.View()` contains `«line»:DrawState` + `> ` prefix on that row; seed 1 filter → `f` `e` Tab-to-Field row, blank it, Enter → `m.View()` shows `«line»:` (conversion); attempt empty Pattern → notice "pattern required" + prompt stays open; attempt bad regex → notice "bad regex" + prompt stays open. | V7,V10,V14,V15,V32,V33,V35 |
| T35 | x | docs: README §Filter Engine — replace bullets to enumerate whole-line, field-scoped, 3 syntax modes (literal/glob/regex), add (`a`), edit (`e`), convert (edit Field row blank/non-blank); README §Keybindings filter-panel row extended w/ `a`/`e`; help overlay `DomainFilterPanel` entries match T34 keyhints footer. SPEC §I.keys already amended in this AMEND round. | V35 |
| T36 | x | [HUMAN tui-mcp] verify on `logs/small.log` × {tokyo-night, catppuccin-mocha} × {below, right}: (1) add field-scoped via pane click → row `+ field:val`; (2) panel `a` → add whole-line `DrawState` exclude → row `− «line»:DrawState`; (3) `e` on existing → edit Pattern → re-render w/ new pattern; (4) `e` → Tab to Syntax row, ←/→ cycle to glob → Tab to Pattern, edit to `user-*-login` → row carries `·glob` dim suffix; (5) `e` → blank Field row → convert to whole-line, panel re-renders w/ `«line»`; (6) attempt empty Pattern → notice "pattern required" + prompt stays open; (7) attempt `Syntax=Regex` w/ `[invalid` → notice "bad regex: <err>" + prompt stays open; (8) disable a filter via Space → confirm whole row Dim; (9) verify `+`/`−` colors visually distinct against each theme; (10) verify cursor visible across all 4 prompt rows — Tab cycles `> ` prefix + bg between rows; text rows carry `█` caret; enum rows carry `[active]` + `◂ ▸` cycle hint only when focused; footer key-hints adapt; (11) press `e` from panel → prompt overlay opens, panel cursor disappears (no dual-cursor); Esc → panel cursor restored at last-navigated row. | V7,V15,V32,V33,V35 |
| T37 | x | render-width: add `safeTruncate(s string, cells int) string` (lipgloss.Width + rune iter) in `internal/ui`; replace byte-slicing in `entrylist/row.go` `visiblePrefixLen` (`len(AbbreviateLogger(...))` :68,:122) + msg-fit (`len(msg) > remaining`) + raw-text `raw[:width]`; `ui/filter/prompt.go` insert/backspace via `[]rune` round-trip. tests: CJK + emoji × ANSI-styled fixtures × {truncate, prefix-pad, caret-after-text}, assert `lipgloss.Width(out) == cells`. | V36 |
| T38 | x | tail-survival: `tail.go` seek-to-offset (| persistent reader) before per-drain read + detect file size-shrink ⇒ reset `pending` + reopen by path; wrap drain IO-err in `TailErrMsg{Err, Retryable}` + exp-backoff reopen on Retryable (cap 5s, max 3) — `TailStopMsg` only non-recoverable; `loader.go` missing-file ⇒ `LoadErrMsg`; `loader.go:86` scanner err ⇒ surface (not log-only). integration: rotation fixture (`os.Rename` mid-stream + truncate-in-place + chmod-700 transient) asserts zero line-loss + `[FOLLOW]` survives. | V37,V43 |
| T39 | x | atomic config write: `config.Save` ⇒ tempfile `<dir>/.cfgtmp-<pid>` + `f.Sync()` + `os.Rename(temp, path)`; orphan `.cfgtmp-*` cleanup on Load (best-effort, ignore err). regression: child-proc Save SIGKILL'd between marshal + rename (env-gated instrument), parent asserts post-mortem file = pre-write contents | absent (never partial). | V22,V38 |
| T40 | x | ParseArgs precond: check `os.Stdin.Stat()` err (no nil-deref — treat err as non-pipe); reject piped-stdin + positional-file conflict (stderr msg + nonzero exit); reject `-f` no-pipe no-file. fix pipe write-end leak `main_test.go:40` (close `pw` after capturing `pr`). tests: Stat-err, stdin+file conflict, `-f` redundancy w/ pipe, `-f` w/o pipe-no-file. | V39 |
| T41 | x | `NewFilterSet()` init `fs.savedEnabled = make(map[int]bool)`. shotgun-mutation test: from fresh ctor call each of {Add, Remove, Enable, Disable, ToggleAll, Update} alone w/ representative args ⇒ no panic; assert Add-during-`globallyDisabled` path safe. | V32,V40 |
| T42 | x | layout/cursor edge fixes + tests: `cursor.go:176` empty filtered ⇒ 0; `list.go:550-571` idx round-trip fix (symmetric across pin-out-of-filter); `detailpane/scroll.go:255` empty content ⇒ exactly `h` blanks; `detailpane/model.go:261-282` `ScrollToLine` thread viewport via param (no temp `s.height` mutation). property-test round-trip identity across all pin states, `expected` from ground-truth per V34. | V34,V41 |
| T43 | x | tea-purity + focus-sync: move `m.list.Focused`/`m.list.Alone` writes from `View()` (`model.go:840-852`) to the appropriate Update branches; on pane-close (incl drag-collapse `model.go:683-747`) sync `m.focus` → visible peer in the same Update step; preserve `m.paneSearch` across `appendToList` re-sync (`model.go:938-943`). tests: View-determinism (call 2×, byte-equal); post-close key routes to visible pane; pane-search query + scroll survive new-entry append. | V12,V13,V42 |
| T44 | x | err-surface sweep: `saveConfig` (`model.go:810`) log + UI notice; `tail_stdin.go:106` drain `readerDone` unconditionally before return + emit `TailErrMsg` if non-nil; ratio-key no-op (`model.go:517-524`) ⇒ user notice. grep non-test `_ =` err-returning calls + annotate intentional drops `//nolint:errcheck <reason>`. | V17,V43 |
| T45 | x | filter validation: `FilterSet.Validate` reject `strings.TrimSpace(Pattern) == ""` ("pattern required"); `prompt.go:172` Edit-commit check `fs.Update(id, f) bool` return — `false` ⇒ `FilterRejectedMsg{"filter not found"}` + keep prompt open. tests: whitespace-Pattern reject; Update-miss keeps prompt open + notice. | V15,V35 |
| T46 | x | tidy (no §V): delete dead `OpenPromptMsg` branch (`model.go:393-400`); `ratiokeys.go:121-136` early-return `usable <= 0`; `mouse.go:82-90` guard `ListContentWidth() < 1` ⇒ ZoneUnknown; align `help.go:53` wording w/ `model.go:918`; `cycleSyntaxNext/Prev` (`prompt.go:357-362`) derive modulo from Syntax enum count (drop `%3`); `classify.go:15` empty-line defensive test; `tail_test.go:68` 3s silent-skip ⇒ hard-fail | explicit `t.Skip(reason)`; `config.go:80 Load` path-resolution policy (`EvalSymlinks` + `Abs` | doc caller-trust). | — |
| T47 | x | [HUMAN tui-mcp] verify NEW V43 notice paths visually distinct from Dim keyhints row (V25/B1 blend lesson) × {tokyo-night, catppuccin-mocha} (contrast pair): (a) trigger `config.Save` err (read-only config dir | inject) → "config save failed" notice — assert Bold | non-Dim fg vs keyhints; (b) ratio key on closed detail pane → no-op signal notice distinct; (c) filter prompt empty/whitespace Pattern → "pattern required" notice distinct; (d) filter Edit on removed id → "filter not found" notice distinct. per path: tui-mcp launch + trigger + `read_region`/`screenshot` bottom row, assert SGR differs from Dim keyhints. VERIFIED 2026-05-31: (c) captured live (prompt-body notice, persistent — distinct); (a)/(b) blocked by tui-mcp altscreen transient-capture limit (`reference_tui_mcp_transient_notice` — 2s keyhints-row notice not landed in buffer snapshot; `+`/`t` reach app, confirmed live pid) ∴ covered by capture-renderer tests `model_v43_test.go` (`config save failed`/`no detail pane` in `keyhints.View()`); (d) race-only, code-confirmed `prompt.go:176-180`. structural distinctness: all 4 route 1× `WithNotice`→`keyhints.go:97` `Bold(true).Foreground(FocusBorder)` ≠ Dim hints `:87`. | V15,V25,V35,V43 |
| T48 | x | [HUMAN tui-mcp] verify V41 layout edges live × {below, right} on `logs/small.log`: (a) open detail pane on an entry with empty content (| scroll past) → below-mode adjacent panes do NOT shift up 1 row (blank-fill = exactly h rows, B27/B46 region); (b) apply a filter matching zero entries → status reads `0/0` NOT `1/0` (empty filtered cursor contract). tui-mcp screenshot ground-truth Y per V34. VERIFIED 2026-05-31: (a) captured live via tui-mcp `read_region` (90-col below-mode, `logs/clean.log` 6 entries) — list bottom border row 18 + detail top border row 19 adjacent + contiguous, no row shift, detail box fully bordered rows 19-27 w/ blank-fill padding, no overflow (V41 blank-fill=h rows holds, V34 ground-truth read from rendered frame). (b) empty-filter cursor `0/0` code-confirmed `list.go:172-173` (`len(m.visibleIndices())==0 → return 0`) + T42 regression test `idx_roundtrip_test.go:107` `TestCursorPosition_EmptyFiltered_Zero_V41` (asserts `CursorPosition()==0` empty-filtered + empty-list) green; live filter-drive blocked by flaky tui-mcp input-registration race (`f`/send_text intermittently dropped — same altscreen flakiness class as `reference_tui_mcp_transient_notice`). | V34,V41 |
| T49 | . | [HUMAN tui-mcp] verify V42 focus-sync + state-preserve live on `logs/small.log`: (a) open detail pane, focus it, close it via ratio-min | drag-collapse → next `j`/`k` routes to entry list (focus re-homed to visible pane, NOT swallowed by hidden pane); (b) open pane-search `/query` in input mode, append a tail entry (`echo >> ` upstream | `-f`) → search query + scroll position survive the append (not clobbered by re-sync). | V12,V13,V42 |

## §B bugs

| id | date | cause | fix |
|----|------|-------|-----|
| B1 | 2026-04-21 | `y` notice bytes DO reach the pty (confirmed via tui-mcp `read_region` immediately after `send_keys y`). Root cause is visual contrast: `KeyHintBarModel.View()` rendered the notice via `Foreground(m.th.Dim)` — same style as the keyhints row — so text was present in the buffer but visually blended with the dim keyhints during the 2s auto-clear window. Earlier diagnosis ("diff-renderer or line-replace drops the row") was wrong; the bug is perceptual, not a dropped write. V15 violation observable only in live TUI. | fixed in T7 (commit `89cb940`): notice branch in `internal/ui/appshell/keyhints.go:86-89` now uses `lipgloss.NewStyle().Bold(true).Foreground(m.th.FocusBorder)` — distinct from Dim keyhints on all 3 themes. V25 live-buffer tests added in `internal/ui/app/live_buffer_y_notice_test.go` for copied-N / no-marks / clipboard-err paths. |
| B2 | 2026-04-21 | list View() prepends 2-cell mark/pin/wrap prefix onto a row already padded to full `m.width`; sum overflows pane inner width, soft-wraps to 2 terminal rows, pushes list output past ViewportHeight, JoinVertical displaces the filename/counter header slot. V4+V5 violated. Observable: mark any entry whose content fills the row → header row disappears from Y=0. Code site: `internal/ui/entrylist/list.go:714-739`. | subtract prefix len from RenderCompactRow width arg, or reserve a 2-cell prefix column inside RenderCompactRow's padding math — covered by V26 |
| B3 | 2026-04-21 | `handleRatioKey` (`internal/ui/app/model.go:808`) calls `saveConfig()` unconditionally after `NextDetailRatio`; at ratio boundary 0.10/0.80, repeated `-`/`+` keypresses produce no value change (clamp-pin) but advance config mtime every press, inflating disk I/O + log churn. V17 "no-op" was value-only and silent on write side-effects. | guard `saveConfig()` on `newR != current`; covered by V17 strengthening + T6 |
| B4 | 2026-04-21 | SPEC V17 + §I.keys under-documented `\|` as just "ratio" alongside `+/-/=`; actual `cycleDetailPreset` semantics (preset-cycle with off-preset→first-preset fallback) surprised operator at `height_ratio=0.1` → `\|` snapped to 0.30 without orientation flip. Code is intentional (ratiokeys.go:78-85, unit test `TestNextDetailRatio_PipeOffPreset_JumpsToFirst`). No code change. | V17 expanded to enumerate per-key semantics + off-preset→first-preset fallback |
| B6 | 2026-04-21 | `HelpOverlayModel.View()` (`internal/ui/appshell/helpoverlay.go:78-80`) writes `\t` between key and description. bubbletea's line-diff renderer leaves `\t`-skipped cells unchanged from the previous frame, so opening `?` from the list view leaves background bytes at those columns. altscreen (`cmd/gloggy/main.go:111`) does not help — diff is per-frame inside the alt buffer. Observable via tui-mcp snapshot after `?`: `  Ctrl+d:56 INFOHalf page down`, `  Tab:17Cycle focus...`, etc., where `:17`, `:56 INFO`, `13:17` are bytes from the list row that was at that column before the overlay opened. | replace `\t` with space-padding to a fixed column; covered by V28 + T16 |
| B5 | 2026-04-21 | `?` preempts active in-pane search — violates V14. `HelpOverlayModel.Update` (`internal/ui/appshell/helpoverlay.go:52-55`) opens help on `?` unconditionally; `Model.Update` (`internal/ui/app/model.go:158-161`) calls `m.help.Update(msg)` at the top, before `handleKey` can route `?` to the active pane search. test `TestModel_HelpOverlay_PreservesListSearchState` (`internal/ui/app/model_focus_test.go:375-397`) codifies the violation: list search in input mode w/ query `"abc"` → press `?` → assertion `m.help.IsOpen()` passes + search preserved on `esc` cycle. V14 reserves `q/Tab/?/Esc` + exempts only `Tab`/`Esc`; `?` has no exemption, so it MUST append to the query like `q` does. | move help-overlay interception after pane-search routing OR gate overlay `Update` on search-input-mode; flip the test to assert `?` → query `"abc?"` + split the help-open path into a no-active-search scenario — covered by V14 strengthening + T15 |
| B7 | 2026-04-23 | T27 HUMAN-verify steps "add 2+ filters via `f`" presuppose runtime add-filter flow; actual: `f` opens filter panel (j/k/Space/d, no add). `uifilter.PromptModel` (`internal/ui/filter/prompt.go`) + `detailpane.FieldClickMsg` (`internal/ui/detailpane/fieldclick.go`) both exist w/ passing unit tests, but `internal/ui/app/model.go` Update has no `case detailpane.FieldClickMsg` + never constructs prompt. README:58 claims "click field → filter prompt" — UX documented + unit-tested isolation, ⊥ wired e2e. ∴ running gloggy ⊥ produce non-empty FilterSet via keyboard \| mouse ∴ V32(b)/(c) HUMAN-verify blocked. Discovered during T27 tui-mcp attempt 2026-04-23. | wire `FieldClickMsg → PromptModel.Open → prompt overlay → Enter → FilterConfirmedMsg → refilter` via T28; post-wire, T27 replayable. recurrence caught by V33 (integration coverage invariant). |
| B8 | 2026-04-23 | T28 `Layout.DetailPaneContentTopY` (`internal/ui/appshell/layout.go`) below-mode formula `ListContentTopY() + EntryListHeight() + 1` double-counts list top border — `ListContentTopY()` = `HeaderHeight + 1` already includes it. Returned 22 for 80x30 below DetailPaneHeight=9; actual pane content starts Y=21 (rendered `{` glyph). Unit tests (`TestLayout_DetailPaneContentTopY_Below`, `TestLayout_ClickToPaneRow_*`) asserted the impl's return value ∴ test + impl self-agreed. Integration test `TestModel_FilterPrompt_MouseClick_Enter_AddsFilter_V33` used `DetailPaneContentTopY()` to compute click Y + `ClickToPaneRow()` reverse-mapped → round-trip cancellation hid the off-by-one from `go test ./...`. Surfaced during T27 tui-mcp HUMAN verify: click at Y=23 (visibly `"level": "INFO"` row) opened prompt pre-filled `time:2026-04-15T13:17:54...` — field one row above visible click. | fix: formula → `HeaderHeight + EntryListHeight + 1` (right-split branch unchanged at `HeaderHeight + 1`), unit-test expectations cross-referenced against live-rendered snapshot (ground-truth Y decoupled from impl). Commit `a575270`. Recurrence covered by V34 (ground-truth test reference invariant). |
| B9 | 2026-04-23 | `TestModel_FilterPrompt_MouseClick_Enter_AddsFilter_V33` (`internal/ui/app/model_filter_prompt_test.go:133`) round-trips ≥ 2 layout helpers (`DetailPaneContentTopY` → `msg.Y` → `ClickToPaneRow`) but helper `findFieldLineY` (:114-125) computes terminal Y via the same `DetailPaneContentTopY()` under test → `paneY = msg.Y - DetailPaneContentTopY()` cancels formula errors inside the harness ∴ a future regression of the B8-class (wrong `DetailPaneContentTopY` formula) would leave `go test ./...` green while live TUI clicks miss their row — the exact failure mode V34 was added to prevent. V34(a) satisfied by `TestLayout_DetailPaneContentTopY_Below` (arithmetic + tui-mcp cross-reference); V34(b) unmet — no integration assertion resolves Y from actual rendered output. Surfaced by `/ck:check` (2026-04-23). | no new invariant — V34 already covers the pattern. Fix via T29: resolve target field's terminal Y by scanning a rendered frame (`m.View()` split on `\n`, locate first line matching `"<field>":`), return that line's index as the absolute click Y. Removes `DetailPaneContentTopY()` from the test's Y-computation path ∴ round-trip now exercises the helper instead of cancelling through it. |
| B10 | 2026-04-23 | `Model.View()` (`internal/ui/app/model.go:812-863`) never invokes `m.filterPanel.View()`. `f` (list-focus branch `model.go:611-621`) sets `m.focus = FocusFilterPanel` + routes subsequent keys to `m.filterPanel.Update` (line 558-566), but the panel's rendered content never reaches `m.layout.Render()` ∴ in live TUI `f` is a silent focus-flip — filter list invisible. `uifilter.Model.View()` has isolated unit tests (`internal/ui/filter/panel_test.go`); `go test ./...` green. README:62 documents `f` as "opens an overlay listing active filters". Discovered 2026-04-23 by user post-T28 walkthrough ("pressing f I see no panel"). Same isolation-vs-integration gap as B7 but on the VIEW axis — V33 as-originally-written covered only UPDATE-axis (`case <Msg>:` in `app.Update`) + missed view-composition drift. | V33 amended to cover both axes (UPDATE + VIEW). Fix via T30: render `m.filterPanel.View()` as full-screen overlay when `m.focus == FocusFilterPanel` (mirror `HelpOverlayModel`/`ThemeSelectorModel` pattern per V28); integration test asserts `m.View()` contains panel glyphs after `f`, restored after `Esc`; empty-state copy updated to surface the add-filter flow (click field in pane). |
| B11 | 2026-04-23 | `FilterSet` state-machine leak: during `globallyDisabled=true`, panel `Space` → `fs.Enable(id)`/`fs.Disable(id)` (`internal/filter/filter.go:76-95`) mutates `f.Enabled` directly but leaves `globallyDisabled` + `savedEnabled` untouched. Next `F` press hits the "2nd-press restore" branch (`filter.go:139-151`) ∴ silently clobbers the user's Space action AND re-enables filters the user believed they had cleared. Repro via tui-mcp 2026-04-23 (session 21): seed 2 filters both enabled → `F` (both `[ ]`, `globallyDisabled=true`, `saved={level:true, thread:true}`) → `Space` on level (`[x]/[ ]`, saved unchanged) → `F` → `[ ]/[x]` (expected: "clear all"; actual: level clobbered + thread re-enabled). User perception: "F not always clearing all the filters." V32 as-written covered 1st-press / 2nd-press / add-while-disabled; interleaved manual toggle was undefined ∴ spec + impl silently diverged. | fix: `fs.Enable`/`fs.Disable` exit the global-toggle state machine when `globallyDisabled=true` — clear `globallyDisabled` + `savedEnabled` after mutating `f.Enabled`. Subsequent `F` restarts the 1st-press cycle from the new current state. V32 amended to document the exit-rule invariant. T31 tracks impl + tests. |
| B12 | 2026-05-30 | `entrylist/row.go:68` `visiblePrefixLen` sums `len(loggerStr)` = byte-len of raw (unstyled) `AbbreviateLogger` output. multibyte logger (CJK/emoji) byte-len > cell-width ∴ msg over-truncated. (review mis-stated as ANSI-styled; confirmed raw via code read 2026-05-30.) | fix T37: cell-width via `lipgloss.Width`/rune-count. recurrence V36. |
| B13 | 2026-05-30 | `entrylist/row.go:122` `RenderCompactRowWithBg` `visiblePrefixLen` uses `len(AbbreviateLogger(...))` raw byte-len while loggerStr rendered styled at :119 ∴ same multibyte over-truncate. | fix T37. recurrence V36. |
| B14 | 2026-05-30 | `entrylist/row.go:42-79` msg-fit `len(msg) > remaining` + `raw[:width]` use byte positions ∴ multibyte rune cut mid-byte → invalid UTF-8 / wrong fit. | fix T37 rune-aware. recurrence V36. |
| B15 | 2026-05-30 | `ui/filter/prompt.go:207-214` backspace computes `len([]rune(field))-1` (rune-aware) but insert (~:224,:226) appends raw byte ∴ buffer rune/byte drift + caret mis-place on multibyte input. | fix T37 `[]rune` round-trip. recurrence V36 (+ V35 AMEND). |
| B16 | 2026-05-30 | `logsource/tail.go:76` fresh `bufio.Reader` per drain caches bytes internally ∴ stale file offset; next drain re-reads | skips on rotation. | fix T38 seek-to-offset | persistent reader. recurrence V37. |
| B17 | 2026-05-30 | `logsource/tail.go:92` `pending` partial-line buffer survives file truncation/rotation ∴ pending + shrunk file = corrupt | lost line. | fix T38 size-shrink detect + reset + reopen. recurrence V37. |
| B18 | 2026-05-30 | `logsource/tail.go:113` drain IO err (perm-denied transient, fd revoked) closes tail w/ no retry/reopen ∴ lines lost, `[FOLLOW]` drops w/o user signal. | fix T38 `TailErrMsg{Retryable}` + backoff reopen. recurrence V37,V43. |
| B19 | 2026-05-30 | `logsource/loader.go:30` `os.Open` err discarded ⇒ `LoadDoneMsg(0)` ∴ missing file shows "loaded empty", no err surfaced. | fix T38 emit `LoadErrMsg`. recurrence V37,V43. |
| B20 | 2026-05-30 | `config/config.go:273` `os.WriteFile(path, data, 0o644)` truncates in-place ∴ crash | power-loss between truncate + write corrupts/empties config. | fix T39 tempfile + Sync + Rename. recurrence V38. |
| B21 | 2026-05-30 | `cmd/gloggy/main.go:59` `stdinStat,_ := os.Stdin.Stat()` then `stdinStat.Mode()`; on Stat err stdinStat=nil ⇒ nil-deref panic (review claimed "silently 0" — wrong, panics; confirmed via code read). | fix T40 check err, treat as non-pipe. recurrence V39. |
| B22 | 2026-05-30 | `cmd/gloggy/main.go:64-66` `case fs.NArg()==1` matches even when stdin piped (first case requires NArg==0) ∴ `gloggy file < pipe` silently ignores stdin + reads file. | fix T40 reject conflict stderr + nonzero exit. recurrence V39. |
| B23 | 2026-05-30 | `cmd/gloggy/main_test.go:40` pipe write-end `pw` from `os.Pipe()` never closed ∴ fd leak across `go test -count=N`. | fix T40 close `pw` after capturing `pr`. |
| B24 | 2026-05-30 | `internal/filter/filter.go:80` `Add` writes `fs.savedEnabled[id]=f.Enabled` under `globallyDisabled` w/o nil-guard; `NewFilterSet:70` returns `&FilterSet{}` (nil map), map inits only in `ToggleAll:211`. unreachable today (flag set only in ToggleAll which inits first) ∴ latent nil-panic for future call-graph change. (review tagged CRIT; downgraded HIGH — not currently reachable.) | fix T41 init map in `NewFilterSet`. recurrence V40. |
| B25 | 2026-05-30 | `ui/entrylist/cursor.go:176` `CursorPosition` sums `len(visible)+1` w/o empty guard ∴ empty filtered list ⇒ 1 not 0 (status shows "1/0"). | fix T42 empty-list guard. recurrence V41. — RECLASSIFIED false-positive (B46): code already correct, T42 added regression test only. |
| B26 | 2026-05-30 | `ui/entrylist/list.go:550-571` `entryIndexForVisible`/`visibleIndexForEntry` asym across pin-out-of-filter ∴ round-trip identity broken in narrow case → wrong-row scroll/cursor w/ pinned out-of-filter entry. | fix T42 round-trip property-test + fix. recurrence V41. — RECLASSIFIED false-positive (B46): code already correct, T42 added regression test only. |
| B27 | 2026-05-30 | `ui/detailpane/scroll.go:255` empty content ⇒ `Repeat("\n", h-1)` = h-1 blanks ∴ off-by-one, adjacent panes shift up 1 row (below-mode). | fix T42 fill exactly h. recurrence V41. — RECLASSIFIED false-positive (B46): code already correct (`Repeat("\n",h-1)`=h rows), T42 added regression test only. |
| B28 | 2026-05-30 | `ui/detailpane/model.go:261-282` `ScrollToLine` assigns `s.height=viewport`, `SetCursor`→`followCursor` uses mutated height, then restores `s.height=ContentHeight()` ∴ cursor land context wrong, post-restore offset outside viewport bounds. | fix T42 thread viewport via param. recurrence V41. |
| B29 | 2026-05-30 | `ui/app/model.go:840-852` `View()` writes `m.list.Focused`/`m.list.Alone` before list render ∴ bubbletea multi-call-per-frame ⇒ stale state. purity violation. | fix T43 move writes to Update. recurrence V42. |
| B30 | 2026-05-30 | `ui/app/model.go:683-747` drag-release closes pane mid-drag but never resyncs `m.focus`; if focus was `FocusDetailPane` ∴ stays on hidden pane → next key mis-routed. | fix T43 sync focus → visible peer on close. recurrence V12,V42. |
| B31 | 2026-05-30 | `ui/app/model.go:938-943` `appendToList` re-syncs pane to new entry on append ∴ clobbers active pane-search query + scroll (user `/query` open, tail entry arrives → query lost). | fix T43 preserve `m.paneSearch`. recurrence V13,V42. |
| B32 | 2026-05-30 | `ui/app/model.go:810` `_ = config.Save(...)` swallows err ∴ disk-full / perm err vanishes, stale theme/ratio post-restart, no explanation. | fix T44 log + notice. recurrence V43. |
| B33 | 2026-05-30 | `logsource/loader.go:86` scanner err logged not returned ∴ partial load shows "done". | fix via T38 (surface as `LoadErrMsg`). recurrence V43. |
| B34 | 2026-05-30 | `logsource/tail_stdin.go:106` `readerDone` err read ONLY in `==context.Canceled` branch; if `ctx.Done():97` wins race goroutine returns w/o draining ∴ err leaked. | fix T44 drain unconditionally + `TailErrMsg`. recurrence V43. |
| B35 | 2026-05-30 | `ui/app/model.go:517-524` ratio keys silent no-op when pane closed — no user feedback (V17 silent-noop on write side complemented by missing signal). | fix T44 emit notice. recurrence V17,V43. |
| B36 | 2026-05-30 | `internal/filter/filter.go Validate` checks `f.Pattern == ""` ONLY ∴ whitespace-only Pattern passes → filter matches/excludes ~everything (spurious whitespace). confirmed via code read. | fix T45 `TrimSpace` reject "pattern required". recurrence V35 AMEND. |
| B37 | 2026-05-30 | `ui/filter/prompt.go:172` Edit-commit `m.fs.Update(id, f)` return discarded; if id removed between OpenEdit + Submit ⇒ Update false, prompt closes clean, no mutation, no signal. confirmed via code read. | fix T45 check return + "filter not found" notice + keep open. recurrence V35 AMEND. |
| B38 | 2026-05-30 | `ui/app/model.go:393-400` global `case uifilter.OpenPromptMsg` dead — `FocusFilterPanel` case (:583-598) resolves msg eagerly + returns ∴ unreachable. maintenance trap (divergent dup). | fix T46 delete dead branch. |
| B39 | 2026-05-30 | `ui/appshell/ratiokeys.go:121-136` `RatioFromDragX` `float64(detail)/float64(usable)` no `usable>0` guard ∴ usable==0 ⇒ +Inf, post-hoc ClampRatio masks (fragile). | fix T46 early-return guard. |
| B40 | 2026-05-30 | `ui/appshell/mouse.go:82-90` `listRightBorder = ListContentWidth()-1` ⇒ -1 when width 0 ∴ `x < listRightBorder` never true → list zone collapsed (degenerate terminal). | fix T46 guard width<1 ⇒ ZoneUnknown. |
| B41 | 2026-05-30 | `ui/appshell/help.go:53` help text "Delete highlighted filter" vs `model.go:918` keyhints "Delete filter" — wording drift. | fix T46 align. |
| B42 | 2026-05-30 | `ui/filter/prompt.go:357-362` `cycleSyntaxNext`/`cycleSyntaxPrev` hardcode `%3` ∴ Syntax enum growth silently breaks cycle (skip | mis-wrap). | fix T46 derive modulo from enum count. |
| B43 | 2026-05-30 | `logsource/classify.go:15` no empty-line bounds-check before `line[0]`; callers strip `\n` ∴ exact "\n" ⇒ len-0 lineCopy. not a known panic; invariant unverified. | fix T46 defensive test. |
| B44 | 2026-05-30 | `tests/integration/tail_test.go:68` 3s timeout silently "skips" (passes) on fsnotify delay / unsupported env ∴ slow-CI masks tail-mode failure as a pass. | fix T46 hard-fail | explicit `t.Skip(reason)`. |
| B45 | 2026-05-30 | `config/config.go:80` `Load(path)` no symlink / `..` resolution; safe today (DefaultConfigPath) but custom `--config` / env path (future) ⇒ traversal | symlink-write risk. | fix T46 `EvalSymlinks` + `Abs` | doc caller-trust contract. |
| B46 | 2026-05-31 | review-sweep (2026-05-30) backprop'd B25/B26/B27 as code bugs w/o reproducing. T42 verification: all 3 already-correct — B25 empty-guard present (`list.go:171`), B26 idx round-trip already identity (all pin states), B27 `Repeat("\n",h-1)`=h rows (newline-vs-row miscount). only B28 reproduced + fixed. V41 "EXACTLY h blank lines (not h-1)" clause carried the same miscount ∴ risked a "fix" breaking correct code. | amend V41 blank-fill clause (rows vs newlines explicit); V44 reproduce-before-fix; B25/B26/B27 reclassified false-positive (code unchanged, T42 regression tests retained). |

(historical backprops recorded in kit changelogs: F-013/F-015/F-016/F-017, F-101..F-109, F-121..F-129, F-132/F-133/F-134, F-200/F-201/F-202.)
