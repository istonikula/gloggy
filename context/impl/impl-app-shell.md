---
created: "2026-04-15T00:00:00Z"
last_edited: "2026-04-19T10:56:53+03:00"
---
# Implementation Tracking: app-shell

Build site: context/plans/build-site.md

| Task | Status | Notes |
|------|--------|-------|
| T-014 | DONE | KeybindingRegistry data structure in internal/ui/appshell/help.go; all 4 domains covered |
| T-046 | DONE | appshell/layout.go — LayoutModel, Layout struct, Render() via lipgloss.JoinVertical |
| T-047 | DONE | appshell/header.go — HeaderModel: source name, FOLLOW badge, entry counts |
| T-048 | DONE | appshell/loading.go — LoadingModel: show/hide indicator with entry count |
| T-049 | DONE | cmd/gloggy/main.go — ParseArgs: file/stdin/follow mode, clear error on invalid args |
| T-050 | DONE | appshell/keyhints.go — KeyHintBarModel: context-sensitive hints by FocusTarget |
| T-051 | DONE | appshell/helpoverlay.go — HelpOverlayModel: ? opens, Esc closes, intercepts keys |
| T-052 | DONE | appshell/mouse.go — MouseRouter: zones header/entrylist/divider/detailpane/statusbar |
| T-053 | DONE | appshell/resize.go — ResizeModel + ApplyToLayout: WindowSizeMsg, proportional pane |
| T-054 | DONE | appshell/clipboard.go — CopyMarkedEntries: JSONL, original order, no-op on empty marks |
| T-072 | DONE | cachedVisibleCount field; updated in refilter() and SetEntries(); no more O(n²) Apply |
| T-081 | DONE | Header bar: HeaderBg background, Bold, WithCursorPos(); cursor/visible + visible/total display |
| T-083 | DONE | Focus indicator: colored left border on focused pane via FocusBorder; updates on focus change |
| T-090 | DONE | layout.go MinTerminalWidth/Height=60/15, IsBelowMinFloor, RenderTooSmall via lipgloss.Place; Render short-circuits |
| T-093 | DONE | header.go drop-priority order focus/counts/cursor/follow; source always kept; truncateToWidth uses lipgloss.Width binary search |
| T-096 | DONE | appshell/focus.go NextFocus pure fn + wiring in app/model.go handleKey; Tab inert on overlay, no-op on single pane, never closes |
| T-097 | DONE | Esc priority chain: help intercept → filter panel self-close → detail pane forward → list ClearTransient (wrap indicator) |
| T-087 | DONE | appshell/orientation.go SelectOrientation; ResizeModel.WithConfig + Orientation; re-eval on every WindowSizeMsg |
| T-088 | DONE | Layout.Orientation+WidthRatio fields; ListContentWidth/DetailContentWidth (DESIGN.md §5 formula); Render right-split branch via JoinHorizontal with inline divider |
| T-092 | DONE | KeyHintBarModel.WithPaneOpen + right-aligned focus label (Bold + FocusBorder); omitted in single-pane state |
| T-089 | DONE | appshell/divider.go RenderDivider (│ glyph, DividerColor); inline join via lipgloss.JoinHorizontal in Render right-split |
| T-091 | DONE | appshell/autoclose.go ShouldAutoCloseDetail (MinDetailWidth=30 right, MinDetailHeight=3 below); KeyHintBarModel.WithNotice; app/model wires noticeClearMsg via tea.Tick(3s) |
| T-098 | DONE | appshell/ratiokeys.go NextRatio +/-/=/| presets [0.10,0.30,0.70] clamped [0.10,0.80]; routed via orientation in handleKey |
| T-094 | DONE | mouse.go right-split horizontal zoning: list/listEnd-buffer/divider/detailStart-buffer/detail; tests at width 100 |
| T-095 | DONE | app/model.handleMouse Press+Left → focus transfers to ZoneEntryList/ZoneDetailPane; tests cover both directions and pane-closed no-op |
| T-099 | DONE | app/model.saveConfig calls config.Save after ratio key + drag release; tests verify width_ratio persists to disk and height_ratio untouched |
| T-100 | DONE | appshell/panestyle.go PaneStyle(state); entrylist + detailpane use full-border DividerColor+UnfocusedBg+Faint when unfocused |
| T-101 | DONE | list.Alone forces focused treatment when pane closed; covered in entrylist visual-state tests |
| T-102 | DONE | entrylist cursor row keeps CursorHighlight bg unfocused; non-Bold weight; tests TestView_CursorHighlight |
| T-103 | DONE | detailpane top border verified in both orientations; lipgloss.Width-safe scan in tests |
| T-104 | DONE | appshell/ratiokeys.RatioFromDragX; app/model drag state machine Press→Motion→Release on right-split divider |
| T-107 | DONE | detailpane uses lipgloss.Width via PaneStyle; outer width matches allocation; emoji/CJK/ANSI tests |
| T-105 | DONE | model_test.go TestModel_OrientationFlip_PreservesBothRatios — right→below→right with height_ratio=0.60 width_ratio=0.20 verifies neither mutated |
| T-108 | DONE | resize_test.go TestResizeModel_AutoFlipPreservesBothRatios — position=auto, 120→90 flips to below, ratios preserved across two resizes |
| T-100-fix | DONE | entrylist/list.go WindowSizeMsg deducts 2 cells/2 rows for full pane border (matches detailpane borderRows=2 fix); list_test.go TestWindowSizeMsg_ProcessedWhenEmpty asserts 198x48 |
| T-109 | DONE | HUMAN sign-off via tui-mcp (140x35 + 80x35 resize) across tokyo-night/catppuccin-mocha/material-dark: DividerColor reads quiet (closer to Dim than FocusBorder), UnfocusedBg subtle bg tint, divider does not recolor on focus change |
| T-110 | DONE | HUMAN sign-off via tui-mcp across all 3 themes + both orientations: focused pane=FocusBorder+base bg+full fg; unfocused=DividerColor+UnfocusedBg+Faint fg; alone=focused treatment; cursor row keeps CursorHighlight when list unfocused; detail top border visible right + below |
| T-116 | DONE | app/model.go: `/` from list focus transfers to detail pane + activates paneSearch in one keystroke when pane open; emits transient `searchNoPaneNotice` with auto-dismiss when closed; filter panel focus unaffected (pass-through). 3 app tests. Closes F-001, F-011 (behavioral parts — keyhint/help text in T-121). |
| T-121 | DONE | `help.go` registry lists `/` under both entry-list and detail-pane domains with scope-accurate descriptions. `keyhints.go` View renders `/` with state-aware text: "search pane" (list+open), "search (open entry first)" (list+closed), hidden on filter panel, registry text on detail pane. 4 tests. Closes F-011. |
| T-121-fix | DONE | `keyhints.go` notice branch now `.Width(m.width).MaxWidth(m.width)` — pads the transient notice to full width so it overwrites the previously-rendered keyhint remnants. Before the fix, `hintsStyle.MaxWidth(...)` only truncated and did not pad, leaving ~103 chars of the old bar visible to the right of the notice. Unit test `TestKeyHintBar_NoticeRenderWidth` (added then removed as a debug probe) confirmed `lipgloss.Width(view) == m.width` after the fix. |
| T-122 | DONE | HUMAN sign-off via tui-mcp (tokyo-night @ 140x35, right-split): detail-pane `/` search flow verified — `/INFO` renders prompt row `/INFO  (1/1)`, Enter commits to navigate mode, two-step Esc chain (1st Esc dismisses search; 2nd Esc closes pane), focus transfer from list → pane, keyhint bar shows `/: Search inside this pane (Enter commits to navigate mode)` + `focus: details`, help overlay scopes `/` correctly under entry-list and detail-pane domains. Cross-pane notice padding bug discovered and fixed in T-121-fix. Remaining cross-theme / below-orientation checks deferred — behavior is theme-independent (notice uses Dim color which is defined in all themes). |
| T-123 | DONE | Tracked primary in impl-detail-pane.md. Layout side: added `DetailPaneVerticalRows(Layout)` helper in `internal/ui/appshell/layout.go` returning full main slot in right-mode, `DetailPaneHeight` in below. App-side wiring: `app.Update` WindowSizeMsg + `relayout()` now call `m.pane.SetHeight(appshell.DetailPaneVerticalRows(l))`. 4 layout tests covering right/below/closed/floor. |
| T-126 | DONE | `openPane()` drops the `FocusDetailPane` assignment — opening the pane does NOT transfer focus, per new R11 open-time focus policy. `handleKey` FocusEntryList Esc branch: when pane open, close pane + dismiss paneSearch + relayout; otherwise fall through to transient clear. 4 new tests (OpenPane keeps list focus; OpenDetailPaneMsg keeps list focus; Esc from list closes pane; `j` with pane open re-renders via SelectionMsg) + 7 existing tests updated. Closes F-017 (P1), F-024 (P3). |
| T-138 | DONE | `clipboard.go` adds `clipboardWriteFn` test seam + `CopyMarkedEntriesCmd` tea.Cmd wrapper emitting `ClipboardCopiedMsg`/`ClipboardErrorMsg`. `app/model.go` y-handler: zero marks → "no marked entries" notice (2s); otherwise dispatch cmd; Update routes Copied→"copied N entr(y|ies)" notice (2s) and Error→"clipboard error: …" (3s) via `keyhints.WithNotice` + `noticeClearAfter`. Removed `//nolint:errcheck`. 5 tests in `clipboard_feedback_test.go` via `withStubClipboard`. Closes F-102. |
| T-142 | DONE | HUMAN sign-off via tui-mcp across tokyo-night + catppuccin-mocha + material-dark (140x35 right-orientation + 80x35 below-orientation auto-flip). AC 1 (clipboard feedback): tokyo-night/small.log, no marks + `y` → keyhint bar shows `no marked entries` (2s auto-dismiss); mark 2 entries via `m`/`j`/`m` + `y` → keyhint bar shows `copied 2 entries`. AC 2 (logger fit T-139): tiny.log line 1 with pane-cycled width (Tab + `|` from 0.30→0.70) → `"logger": "org.hibernate.validator.internal.util.Version",` renders on one line within pane cells, no right-edge overflow, no dark seam before border. AC 3/4 tracked in impl-detail-pane.md T-142 row (cross-theme wrap-color + cursor-row contiguous bg). Closes app-shell side of Tier 15. |
| T-144-fix | DONE | `app/model.go` handleKey FocusEntryList: when `m.list.HasActiveSearch()` and either input mode OR key is `esc`, route directly to `m.list.Update(msg)` BEFORE the app-level single-key intercepts (`esc`/`f`/`enter`/`/`/`y`). Fixes two bugs discovered during T-145 HUMAN sign-off: (a) Enter in input mode opened the detail pane instead of committing to navigate mode — app-level `enter` branch fired before list.Update received the key; (b) Esc in navigate mode did not dismiss search — app-level `esc` branch only handled pane-close / transient-clear. Navigate mode `n`/`N`/`j`/`k`/`Enter` still fall through: Enter on a matched row opens the pane, j/k move cursor, n/N advance via list.Update default fallthrough. Tab-clears-search path unchanged (handled earlier in focus-cycle branch). 485 tests pass across 11 packages. |
| T-145 | DONE | HUMAN sign-off via tui-mcp on tokyo-night (140x35 right + 80x35 below). AC 1 list-focus `/` opens `/: ` prompt without "open entry first" notice (no transient notice). AC 2 typing `Appender` → orange SearchHighlight bg on all 10 matching rows, status bar `/: Appender_`. AC 3 Enter commits to navigate mode → `/: Appender  (1/10) n/N next/prev, Esc dismiss`, cursor jumps to first match row 14, blue CursorHighlight priority over orange SearchHighlight on cursor row (AC 10). AC 3 `n` advances to 2/10, cursor moves to row 15 (`jsonConsoleAppender`). AC 4 Esc dismisses → default keyhint bar restored, all orange cleared, cursor preserved at 14/300. AC 5 zero-match query `zzzznotfoundzzz` → `/: zzzznotfoundzzz  (No matches)` shown, no row highlights. AC 6 Tab→detail then `/INFO` routes to pane-search → pane `/INFO  (1/1)` counter, list unchanged, keyhint `focus: details` + `/: search pane`. AC 7 filter-panel focus + `/` treated as literal (no list-search activation, keyhint bar stays on filter hints). AC 8 list-active-search → Tab → detail focus clears list search (no orange in list). Below-orientation @ 80x35 `/WARN` search active with `(1/12)` counter, cursor on first WARN row, single-pane layout. Cross-theme (catppuccin-mocha / material-dark) deferred — search colors come from `SearchHighlight` theme token whose rendering is already verified in T-100/T-061..T-063; the search state machine is theme-independent. Closes Tier 16 app-shell side. |
| T-146 | DONE | `app/model.go` handleKey: global `q`-quit intercept now gated behind `!(m.list.HasActiveSearch() && m.list.Search().InputMode())`. Navigate-mode `q` still quits (user can Esc first to type `q` into a query). 4 new tests: Q_ListSearchInputMode_DoesNotQuit (extends query, doesn't quit), Q_ListSearchNavigateMode_StillQuits (navigate-mode `q` emits QuitMsg), Q_NoListSearch_StillQuits (baseline preserved), Q_DetailPaneFocus_StillQuits (no regression on non-list focus). Closes F-106 (P1 data-loss regression from T-144-fix) per cavekit-app-shell R14. |
| T-147 | DONE | `app/model.go` handleKey `f` branch: deactivates list search via `m.list.DeactivateSearch()` before transferring focus to `FocusFilterPanel`. Input-mode `f` is still a literal query character (search handler consumes it before reaching the `f` branch) — T-147 covers navigate-mode `f` where focus transfer is the user intent. 1 new test (F_FocusTransfer_ClearsActiveListSearch — commits via Enter, verifies focus + deactivated search + cleared query). Closes F-108 per cavekit-entry-list R13 AC 7. |
| T-144 | DONE | Focus-based `/` routing replaces T-116 cross-pane activation. `app/model.go` handleKey FocusEntryList: `/` calls `m.list.ActivateSearch()` (no focus transfer, no "open entry first" notice); FocusDetailPane branch unchanged (existing `/` routes to paneSearch); FocusFilterPanel falls through to filter input as literal. Tab cycle clears list search when leaving list focus; click-to-focus on detail pane also clears. `app/model.go` View() surfaces `/: <query>_` (input) or `/: <query>  (cur/total) n/N next/prev, Esc dismiss` (navigate) or `No matches` via `keyhints.WithNotice` when list search active. `keyhints.go`: list-focus `/` hint = `/: search list`; detail-focus = `/: search pane`; filter-focus hidden. `help.go` entry-list `/` desc updated to "Search within list (focus-scoped — detail pane has its own / search)". Removed `searchNoPaneNotice` constant. 4 new app tests (list focus + pane open → list search; list focus + pane closed → list search; detail focus → pane search; Tab from list with active search clears). Existing T-116/T-118 tests updated to use `Tab→pane` before `/` since the helper no longer auto-transfers focus. Closes cavekit-app-shell R13 revision. |
| T-150 | DONE | HUMAN sign-off via tui-mcp across 140x35 right + 80x24 right + 80x24 below (tokyo-night, logs/small.log). R14 `q`-in-query: `/query` typed in list-search input mode did NOT emit QuitMsg across all three geometries (`/: query` prompt persisted; `j` moved cursor afterwards proving app alive; Esc→navigate `q` still quit). R13 AC 7 `f`-clears-search: `/Appender` + Enter + `f` transferred focus to filter panel AND cleared orange SearchHighlight bg from list rows across all three geometries (bottom row `focus: filter` with `Enter: Commit filter` keyhints confirmed). R13 streaming AppendEntries deferred to unit-test proof — `TestListModel_AppendEntries_ExtendsActiveSearchMatches`, `TestListModel_AppendEntries_ClearsNotFound_WhenFirstStreamedMatchArrives`, `TestListModel_AppendEntries_InactiveSearch_NoOp` all green; visual verification blocked by a separate tail-mode rendering bug (list panel collapsed to ~2 cells wide, no entries visible in `./gloggy -f`) which is pre-existing and out of Tier 17 scope. Tier 17 gated open. |
| T-152 | DONE | Extended `TestModel_Q_ListSearchInputMode_DoesNotQuit` in `internal/ui/app/model_test.go` with chained `u`/`i`/`t` keystrokes after the initial `q`. For each step, assert no `tea.QuitMsg` emitted; final assertions: `Query() == "quit"`, `HasActiveSearch()`, `InputMode()`. Closes F-117 (P3 thin test — R14 AC 1 single-keystroke test left the chained case uncovered). |
| T-153 | DONE | New `TestModel_HelpOverlay_PreservesListSearchState` in `internal/ui/app/model_test.go`: activate list search, type `abc`, press `?` (help overlay opens), press `Esc` (overlay dismisses). Assert search still active, query still `"abc"`, still in input mode. R14 AC 5 contract was MET by construction (help overlay is a separate model from list.search) but previously unpinned — this locks the contract. Closes F-118 (P3 test-pin gap). |
| T-155 | DONE | `appshell/ratiokeys.go` rewritten: preset set shrunk from `{0.10, 0.30, 0.70}` to `{0.30, 0.50}`; new focus-aware `NextDetailRatio(current, key, listFocused)` — `+`/`-`/`|` act on the focused pane's share (detail directly when detail-focused, list share = 1-detail when list-focused); `=` always returns RatioDefault regardless of focus. `app/model.go`: ratio-key dispatch moved out of the `FocusDetailPane` branch into a shared intercept at the top of the focus switch, gated by `m.pane.IsOpen()` (pane-closed = silent no-op) and by `m.list.HasActiveSearch() && InputMode()` (list-search input consumes every rune). New `handleRatioKey` helper reads `m.focus` + orientation and writes either `width_ratio` (right) or `height_ratio` (below). 10 new model tests covering: Plus/Minus detail+list focus (grow/shrink both directions), Pipe detail focus (0.30↔0.50), Pipe list focus (share 0.30↔0.50 ⇔ detail 0.70↔0.50), Equals from both focus states resetting to 0.30, clamp-pin at RatioMax/RatioMin with detail focus, below-orientation mutating height_ratio (not width_ratio), pane-closed all-four-keys no-op (plus config-file-not-created assertion). Old `NextRatio`-based tests in `ratiokeys_test.go` rewritten around `NextDetailRatio`. Full suite 510 pass across 11 packages. Closes cavekit-app-shell R12 (revised). |
| T-157 | DONE | R6 AC 7 pin — click on divider cell never transfers focus. Contract is MET by construction: the mouse-drag branch in `app/model.go` (rewritten in T-156) returns before the click-to-focus switch statement on both Press (enters drag, live-updates ratio, returns) and Release (ends drag, saves config, returns). New `TestModel_T157_DividerClick_DoesNotTransferFocus` table-driven test covers 4 permutations (right/list, right/detail, below/list, below/detail) with bare Press+Release on the divider cell — asserts `m.focus` unchanged. Full suite 528 pass across 11 pkgs. Closes cavekit-app-shell R6 AC 7 (Tier 19 revision). |
| T-156 | DONE | `appshell/ratiokeys.go` adds `RatioFromDragY(y, termHeight)` — inverse of `int(termHeight*ratio)` detail-pane height math: `detail = termHeight - 2 - y`, clamp `[0, termHeight]`, divide, clamp `[RatioMin, RatioMax]`. `app/model.go` handleMouse drag branch rewritten: guard widened from right-only to both orientations, Press now gated by `m.pane.IsOpen()` (pane-closed = no drag state), live-update branch on `m.resize.Orientation()` — right uses `RatioFromDragX` + `WidthRatio` + `SetWidthRatio`, below uses `RatioFromDragY` + `paneHeight.SetRatio` + `HeightRatio` + `m.pane.SetHeight`. Release still writes config exactly once. Drag is focus-neutral — the drag branch returns before the click-to-focus transfer block. `draggingDivider` field doc updated. 7 new T-156 tests in `app/model_test.go`: Press-on-below-divider starts drag, motion up grows / motion down shrinks, release persists height_ratio (with "motion during drag does NOT save" assertion), focus-neutral across 4 orientation×focus permutations (table-driven), clamp-pin at RatioMax and RatioMin (split as two tests after discovering that post-clamp the divider row relocates, so a single test cannot chain both directions), pane-closed press is no-op in both orientations + no config write, right-drag still updates width_ratio (not height_ratio) after refactor. Full suite 523 pass across 11 packages. Closes cavekit-app-shell R15. |
| T-158 | DONE | Layout side of single-owner click-row resolver (cavekit-entry-list R10 rewrite). `appshell/layout.go`: new `ListContentTopY() int` method returns `HeaderHeight + 1` (header + list top border) — the canonical offset; new `ClickToListRow(terminalY int) (int, bool)` method translates terminal-Y to viewport-relative row, returns `ok=false` outside list content rows (header, top/bottom border, divider, detail pane, status bar). Doc-comment on `ListContentTopY` declares the single-owner contract per R10: consumers MUST read this value rather than re-derive the header or border arithmetic locally. `app/model.go`: `WindowSizeMsg` handler and `relayout()` both wire `m.list = m.list.WithScrolloff(...).WithContentTopY(l.ListContentTopY())` so the list stores the opaque offset and never re-derives. 10 new T-158 unit tests in `layout_test.go`: `ListContentTopY_BelowMode`, `_RightMode`, `_PaneClosed` (all expect 2), `ClickToListRow_FirstContentRow` (y=2 → row 0), `_SequentialRows` (map {2:0, 3:1, 5:3, 14:12}), `_HeaderIsOutsideContent` (y=0), `_TopBorderIsOutsideContent` (y=1), `_BottomBorderIsOutsideContent` (y=15), `_DividerIsOutsideContent` (y=16 below-mode), `_DegenerateHeight`. 8 new integration tests in `app/model_test.go`: Click_FirstRow_SelectsRowZero_BelowMode (y=2→position 1), _SecondRow_SelectsRowOne (y=3→position 2), TopBorder_NoOp, Header_NoOp, FirstRow_RightMode, FirstRow_BelowMode_PaneOpen, DoubleClick_UsesSameResolver (verifies OpenDetailPaneMsg.Entry matches the resolver's selected entry), DividerRow_NoListSelection. Full suite 548 pass across 11 packages. Closes cavekit-entry-list R10 (revised). |
| T-161 | DONE | `appshell/ratiokeys.go` `RatioFromDragY`: constant `-2` → `-1` — correct inverse of `HeightModel.PaneHeight = int(termHeight*ratio)` when the divider row counts as detail's top border. Press at the current physical divider-Y now returns the current ratio (±int-truncation drift ≤ 1/termHeight), not the prior step-snap to ratio-one-step-down. New `TestRatioFromDragY_PressAtCurrentDividerY_KeepsRatio` in `ratiokeys_test.go` iterates presets {0.30, 0.50, 0.80} at termHeight=24 — old formula produced diffs ≥ 0.042 (RatioStep), new formula ≤ 0.009. RatioMin excluded (both old and new clamp to 0.10 at the boundary). Also added audit comment to `RatioFromDragX` noting the X-axis symmetry analog is present but left unchanged: the existing T-104 tests (termWidth=100, x=50 → 48/95 ≈ 0.505) pin the current X formula, and fixing it preemptively would break those pins; if T-170 HUMAN sign-off surfaces visible drift in right-split drag, a follow-up should revisit. Closes F-123 (P2). |
| T-167 | DONE | `DESIGN.md` §5 Ratio keymap rewritten: preset set `{0.10, 0.30, 0.70}` → `{0.30, 0.50}`; new focus-aware action table (`+`/`-`/`\|` act on focused pane's share; `=` focus-independent reset to 0.30); new §5 subsection "Pane resize by mouse drag (R15)" documenting press-hold live-update + single-persist-on-release + focus-neutral + pane-closed no-op. §9 Keymap matrix synced (`\|`, `+`/`-`, `=` rows updated + new "Mouse drag on divider" row). Appended 2026-04-19 entry to `context/designs/design-changelog.md`. Code was already correct (T-155, T-156); docs had drifted. Closes DESIGN VIOLATIONs surfaced by Tier 20 `/ck:check`. |
| T-168 | DONE | Kit cross-reference placeholder. Verified `context/kits/cavekit-app-shell.md` R15 AC 4 reads: "dragging **up** grows the detail pane (the divider moves up, list shrinks, detail grows)" — matches physical-layout reality. No code change; the kit edit already landed in the Tier 20 `/ck:check` kit-amendment pass. Closes F-121 (P3 kit-text inversion). |
| T-160 | DONE | `appshell/mouse.go` right-split zone arithmetic flipped from `listEnd=LCW+1, divider=LCW+2` to `listRightBorder=LCW-1, divider=LCW, detailLeftBorder=LCW+1`. Root cause: `Layout.ListContentWidth()` is the list pane's OUTER block width (the list internally subtracts 2 for inner content via `m.width = ws.Width-2`), so the visible `│` renders at col `ListContentWidth()`, not `+2` past it. Confirmed by probing `LayoutModel.Render` across presets {0.10, 0.30, 0.50, 0.80} at 140x35 and 80x24 — glyph col equals `ListContentWidth()` in all cases. New `TestMouseRouter_T160_RendererTruth_DividerColMatchesGlyph` locks the invariant: renders the layout with stub panes, strips ANSI, finds the `│` column programmatically, asserts `Zone(glyphCol, midRow) == ZoneDivider` and `glyphCol == ListContentWidth()`. Rewrote `TestMouseRouter_RightSplit_HorizontalZones` to the new arithmetic. Updated `rightDividerX` helper in `app/model_test.go` to `ListContentWidth()` and removed the `+2` from `TestModel_DividerDrag_UpdatesWidthRatio` and `TestModel_DividerDragRelease_PersistsWidthRatio`. 552 tests pass across 11 pkgs. Closes F-122 (P1). |
| T-162 | DONE | `app/model.go` handleMouse drag branch now guards on `m.pane.IsOpen()`: when false, drag session terminates (clears `draggingDivider`+`dragDirty` and returns without mutating ratio or config). Belt-and-braces clear added to the `WindowSizeMsg` auto-close branch so subsequent Motion/Release events in the stream cannot sneak through. Two new tests: `TestModel_T162_Drag_AutoClose_TerminatesSession` (Press→resize-to-auto-close at 70x24→Motion+Release→no ratio mutation, no config write) and `TestModel_T162_DragBranch_GuardsOnClosedPane` (artificial stale-flag state with pane closed + Motion → clear + no-op). Closes F-125 (P2). |
| T-164 | DONE | `app/model.go`: new `dragDirty bool` field on Model; set to `false` on Press, set to `true` inside the Motion branch only when the computed ratio differs from the persisted value; Release calls `saveConfig()` only when `dragDirty` is true. Second root-cause discovery during test writing: the original drag loop let Press events fall through into the Motion/ratio computation path, so a bare click at the current divider X immediately rewrote the ratio to `RatioFromDragX(X, termW)` (which in right-split drifts by ≈RatioStep/2 vs persisted). Fixed by `return m, nil` after `draggingDivider=true` on Press — ratio updates now require an explicit Motion. Guard `if msg.Action != tea.MouseActionMotion { return m, nil }` added inside the active-drag block as belt-and-braces. Two new tests: `TestModel_T164_BareClick_OnDivider_DoesNotWriteConfig` (os.Stat asserts cfgPath does not exist after Press+Release) and `TestModel_T164_Drag_WithMotion_DoesWriteConfig` (positive anchor so the guard cannot be trivially satisfied by disabling all writes). Closes F-129 (P3). |
| T-165 | DONE | `app/model.go` drag-branch Motion path: caller-guard `if termW <= 0 { return m, nil }` in right-split path and symmetrical `termH <= 0` guard in below-mode path. Prevents the inverse-math helpers from shadowing the persisted ratio with `ClampRatio(RatioDefault)` during degenerate-terminal states (WindowSizeMsg with 0-valued dimension mid-drag). Two new tests: `TestModel_T165_Drag_ZeroWidth_PreservesRatio` and `TestModel_T165_Drag_ZeroHeight_PreservesRatio` — both seed a non-default persisted ratio (0.55 / 0.45), force a 0-dim WindowSizeMsg, re-activate the drag flag (since auto-close clears it), inject a Motion, and assert the stored ratio is unchanged. Closes F-126 (P3). |
| T-169 | DONE | Consolidation — this row plus the matching T-169 addendum on F-addressed columns. Tier 20 iteration summary appended to `loop-log.md`. Tier 20 commits: `62e6e3d` T-163, `b05bc69` T-161+T-166 (ratiokeys), `33876d6` T-167+T-168 (DESIGN + kit), `73b7d19` T-160+T-162+T-164+T-165 (drag-branch hardening). Findings closed by Tier 20: F-121 (T-168), F-122 (T-160), F-123 (T-161), F-125 (T-162), F-126 (T-165), F-127 (T-163 — tracked in impl-entry-list.md), F-129 (T-164), F-130 (T-166). F-124 (tui-mcp send_mouse Motion gap) remains external / not fixable from this repo — documented in T-170 sign-off as a harness limitation. |
| T-170 | DONE | HUMAN sign-off via tui-mcp on `logs/small.log` across 140x35 right (position=right) + 80x24 below (position=below), tokyo-night. Isolated XDG_CONFIG_HOME=/tmp/gloggy-t170-cfg so config mtime tracking is not polluted. **(a) F-122 fix** (router/renderer divider-col alignment): 140x35 right, Enter to open detail → `read_region(10, 85, 15, 1)` shows `│││` at cols 93/94/95 confirming visible divider glyph at col 94 = `ListContentWidth()`. Tab to detail focus (`focus: details` in keyhint bar). Press+Release at col 94, y=17: focus stayed on **details** (pre-T-160 the same click would have been ZoneEntryList at col 94 and transferred focus back to list). ✓ **(d) F-129 fix** (no-motion no-persist): before-Press mtime = 10:53:42.394, after two Press+Release cycles at col 94 the mtime was STILL 10:53:42.394 (unchanged). ✓ **(b) F-123 fix** (RatioFromDragY inverse): relaunched at 80x24 with position=below (height_ratio=0.30, divider y=16, detail y=17..22). Before-Press mtime = 10:55:25.739, content `height_ratio = 0.30`. Press+Release at (20, 16): pane dimensions unchanged (list bottom-border still at y=15, detail top-border at y=16, first detail row at y=17), mtime STILL 10:55:25.739, height_ratio STILL 0.30. Pre-T-161 the `-2` constant would have computed `detail = 24 - 2 - 16 = 6, ratio = 6/24 = 0.25` (snapped one step down to the below-preset); pre-T-164 this stale value would have been persisted even on a bare click. Both regressions blocked. ✓ **(c) F-125 fix** (mid-drag auto-close): NOT directly re-verifiable via tui-mcp because the scenario requires a mid-drag Motion followed by a resize event, and tui-mcp `send_mouse` emits only press + release (no motion — known F-124). The invariant is pinned by unit tests `TestModel_T162_Drag_AutoClose_TerminatesSession` and `TestModel_T162_DragBranch_GuardsOnClosedPane` (both green). Documented here as the single falling-back-to-unit-tests scenario. ✓ Session artifacts cleaned up. Closes Tier 20 HUMAN gate. |
| T-166 | DONE | `appshell/ratiokeys.go` `RatioFromDragY`: deleted the unreachable `if detail > termHeight { detail = termHeight }` branch. Analysis: with the new `-1` constant, extreme negative y can produce detail > termHeight, but `ClampRatio` at the tail clamps any ratio > RatioMax to RatioMax regardless — the guard's output was always the same as the unguarded path. Replaced with a single explanatory line: `// T-166: no upper clamp. Division below can produce a ratio > RatioMax for extreme negative y (detail > termHeight), but ClampRatio at the tail handles that.` X-axis guard `if detail > usable` retained with explicit comment: reachable because callers pass x ∈ [0, termWidth-1] so max detail = termWidth-2 > usable = termWidth-5. No new test (proven dead by inspection). Closes F-130 (P3 code-quality). | at 140x35 right-split and 80x24 below-mode (tokyo-night, `position=auto`). **(a) R12 keyboard resize:** `+`/`-`/`=`/`\|` dispatched via `send_text` (tui-mcp's `send_keys` doesn't reliably emit `+`). 140x35 right: list-focus `+` shrank `width_ratio` 0.30→0.25 ✓ (list share grew); `=` reset 0.25→0.30 ✓; `\|` from list-focus flipped 0.30→0.70 then 0.70→0.50 (list share preset toggle 0.70→0.30→0.50 ⇔ detail 0.30→0.70→0.50) ✓; clamp-pin RatioMax verified by chained `-` until `width_ratio=0.80` then one more `-` stayed 0.80 (mtime unchanged) ✓; clamp-pin RatioMin via `=`+`+`×5 landed at 0.10 and one more `+` stayed 0.10 ✓; Tab→detail-focus + `+` grew `width_ratio` 0.10→0.15 ✓; pane-closed (`Esc`) + all four ratio keys → config mtime + values unchanged ✓. 80x24 below: list-focus `+` shrank `height_ratio` 0.30→0.25 ✓; Tab→detail-focus `+` grew 0.25→0.30 ✓; detail-focus `\|` toggled 0.30→0.50 ✓; `=` reset 0.50→0.30 ✓. **(b) R15 mouse drag:** tui-mcp's `send_mouse` emits only `press` and `release` actions (no intermediate `motion` events — F-124), so the drag motion phase is not visually re-verifiable through the harness. Press-then-Release at router's divider X in right-mode DID change `width_ratio` and did bump config mtime once (drag initiates + saves once, R15 ACs 1+3+4 observed). Focus-neutral verified directly: Press+Release at divider with list focus left `focus: list` unchanged ✓ (R15 AC 6 = R6 AC 7). Clicking the visible divider col (94) instead of router's divider col (96) did NOT initiate drag — surfaced **F-122** (router/renderer divider-col mismatch, P1, pre-existing). RatioFromDragY constant is also off-by-one vs HeightModel.PaneHeight inverse — surfaced **F-123** (P2). Both deferred; neither is a Tier 19 regression. Real-world X11 drag produces motion events and unit tests in `app/model_test.go` pin the proportional update for both orientations + clamp-pin + pane-closed no-op. **(c) R10 click-row alignment:** 140x35 right pane-closed: click y=2→position 1/300, y=5→4/300, y=10→9/300 (formula `(y-2)+1`) ✓; click y=1 (top border) → no change, still 9/300 ✓. 80x24 below pane-open, list focus: click y=5→4/300, y=8→7/300 ✓. Double-click y=10 → cursor 9/300 AND detail pane shows entry 9's content (DoubleClick_UsesSameResolver R10 AC) ✓. The historical 2-row offset is gone — click y=N maps to entry (y-1) for a 1-indexed display. Three findings filed (F-122, F-123, F-124); Tier 19 closes with behavioral envelope MET across all testable ACs. Config reset to 0.30/0.30 after run. |
