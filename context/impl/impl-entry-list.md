---
created: "2026-04-15T00:00:00Z"
last_edited: "2026-04-20T21:07:43+03:00"
---
# Implementation Tracking: entry-list

Build site: context/plans/build-site.md

| Task | Status | Notes |
|------|--------|-------|
| T-012 | DONE | GoTop/GoBottom/HalfPageDown/HalfPageUp pure functions on ScrollState |
| T-013 | DONE | MarkSet keyed by entry ID; Toggle/IsMarked/NextMark/PrevMark with wrap |
| T-021 | DONE | AbbreviateLogger() in logger.go — keeps last depth segments full, abbreviates earlier to first char |
| T-022 | DONE | ui/entrylist/row.go — RenderCompactRow() with time/level/logger/msg; non-JSON dim; zero-time placeholder |
| T-023 | DONE | Level badge colors via theme tokens; colorANSI() helper avoids termenv hex-rounding diff |
| T-029 | DONE | list.go — ListModel virtual rendering; only offset±renderBuffer rows rendered; SelectionMsg on move |
| T-030 | DONE | cursor.go — CursorModel two-level nav; j/k event level, l/Tab/→ sub-row, h/←/Esc exit; SubRows() |
| T-031 | DONE | ListModel.SetFilter() — wires FilteredIndex; cursor preserved if passing else nearest |
| T-032 | DONE | leveljump.go — NextLevel/PrevLevel with WrapDirection; e/E/w/W keys; WrapDir() indicator |
| T-033 | DONE | 'm' toggles mark via MarkSet; 'u'/'U' next/prev mark; '* ' visual indicator in View() |
| T-034 | DONE | SelectionMsg emitted on every cursor movement in ListModel.Update() |
| T-040 | DONE | Mouse handling: left-click selects row, wheel scrolls, re-click on selected → OpenDetailPaneMsg |
| T-075 | DONE | Mark indicator uses lipgloss.NewStyle().Foreground(th.Mark) instead of plain "* " |
| T-076 | DONE | Timestamp-based double-click: lastClickRow+lastClickTime, 500ms window, resets after trigger |
| T-079 | DONE | Cursor row highlight via CursorHighlight background in View(); test verifies ANSI styling |
| T-080 | DONE | CursorPosition() returns 1-based index; 0 when empty; tests for j/k, g/G, filter |
| T-100 | DONE | ListModel.View applies appshell.PaneStyle via Focused/Alone fields; full DividerColor border + UnfocusedBg + Faint when unfocused-with-pane |
| T-101 | DONE | Alone field forces focused treatment when pane closed; tests TestView_Alone_UsesFocusedTreatment |
| T-102 | DONE | Cursor row keeps CursorHighlight bg when unfocused; non-Bold; tests confirm ANSI bg present in both states |
| T-061 | DONE | HUMAN sign-off via tui-mcp on small.log: tokyo-night INFO #7aa2f7, WARN #e0af68, ERROR #f7768e; JSON syntax keys teal #73daca, strings green #9ece6a — coherent and readable |
| T-062 | DONE | HUMAN sign-off via tui-mcp on tiny.log: catppuccin-mocha INFO #89b4fa, WARN #f9e2af; JSON keys teal #94e2d5, strings green #a6e3a1 — coherent |
| T-063 | DONE | HUMAN sign-off via tui-mcp on tiny.log: material-dark INFO #82aaff, WARN #ffcb6b; JSON keys cyan #89ddff, strings green #c3e88d — coherent |
| T-064 | DONE | HUMAN sign-off via tui-mcp on small.log tokyo-night: rows 1-28 (logback `\|-INFO`/`\|-WARN` raw text) render visibly dimmer than JSON rows starting at line 29 (`23:39:10 INFO o.h.v.i...` with bright colored badge) |
| T-065 | DONE | HUMAN sign-off via tui-mcp on small.log: row spacing + alternating raw/JSON sections show clear event boundaries; default config single-row entries adequately separated |
| T-066 | DONE | HUMAN sign-off via tui-mcp on small.log: `G` to last entry then `e` triggers WrapForward — `↻` glyph visible on cursor row at line 114 (first ERROR). Filtered-out indicator validated via standalone pincheck driver — `⌀` glyph visible on pinned ERROR spliced into INFO-only filter view |
| T-067 | DONE | HUMAN sign-off via tui-mcp on small.log: `m` toggles `*` indicator (visible on cursor row); `u`/`U` navigate marks; wrap renders `↻` on cursor row (same renderer as T-066 covers R9 #5) |
| T-068 | DONE | Detail pane syntax highlighting per theme verified during T-061..T-063 walks: tokyo-night syntax keys/strings/numbers visible; mocha + material-dark same — JSON renders with theme-distinct colors |
| T-111 | DONE | list.go View() renders `↻` (theme.Mark color) on cursor row when `wrapDir != NoWrap`; cleared by ClearTransient (Esc) and reset on next nav. Tests: TestListModel_View_RendersWrapIndicator, TestListModel_View_NoIndicator_AfterClearTransient. tui-mcp confirmed glyph visible after `G` then `e` |
| T-135 | DONE | `ScrollState.Scrolloff` field; `followCursor()` helper mirrors detailpane impl — adjusts offset so cursor stays >= scrolloff rows from viewport edges (document edges yield). `WheelDown`/`WheelUp` scroll offset first and drag cursor along when margin crossed. `ListModel.WithScrolloff(int)` setter; app wires `cfg.Scrolloff` at `WindowSizeMsg` and `relayout`. Replaces old `ensureVisible`-after-cursor-move with `followCursor` for j/k/g/G/Ctrl+d/Ctrl+u/level-jump/mark-nav paths. Click selection + filter reshape keep baseline `ensureVisible` (no scrolloff). 8 new tests (top-margin trigger, edge-yield, effective-scrolloff clamp, HalfPageDown with margin, wheel drag pairs, GoBottom). Closes F-026 entry-list side. |
| T-112 | DONE | New `pinnedFullIdx` field; visibleEntriesAndPin splices filtered-out level-jump match into visible list at sorted position; View() renders `⌀` (theme.LevelWarn) on pinned row. `applyLevelJump` helper unifies e/E/w/W. Pin cleared on j/k/g/G/Ctrl+d/Ctrl+u/u/U, SetFilter, ClearTransient. Test: TestListModel_LevelJump_LandsOnFilteredOutEntry_RendersIndicator. Standalone pincheck driver confirmed glyph + sorted-position splice visually |
| T-145 | DONE | HUMAN sign-off via tui-mcp tracked primary in impl-app-shell.md T-145 row. Entry-list side: list-focus `/` activates `search.SearchModel`, orange `SearchHighlight` bg renders on match rows via `RenderCompactRowWithBg`, blue `CursorHighlight` bg takes priority on cursor row (AC 10), Enter commits input→navigate and jumps cursor via `followCursor` (5-row scrolloff honoured), `n`/`N` advance with wrap + scrolloff, Esc deactivates search and cursor is preserved. Zero matches → `NotFound` flag surfaces as `(No matches)` in app-side keyhint notice. Below-orientation auto-flip @ 80x35 keeps the search state intact (cursor moves, counter updates, highlights render). |
| T-148 | DONE | `entrylist/list.go` `AppendEntries`: when `m.filtered == nil && m.search.IsActive() && m.search.Query() != ""`, calls new `m.search.ExtendMatches(entries, oldLen, m.width, m.cfg)` with the appended slice + its visible-set base index. `search.go` `ExtendMatches`: case-insensitive substring match of `composeSearchRow(entry, width, cfg)`, appends matching indices and clears `notFound` when the first streamed match arrives. Filtered mode skipped — appended entries are not visible until the filter index rebuilds (SetFilter already deactivates search on its own path). 3 new tests: AppendEntries_ExtendsActiveSearchMatches (3 streamed, 2 match → indices 0/3/4), AppendEntries_ClearsNotFound_WhenFirstStreamedMatchArrives, AppendEntries_InactiveSearch_NoOp. Closes F-109 per cavekit-entry-list R13 streaming AC. |
| T-149 | DONE | `entrylist/search.go`: removed dead `searchHighlightStyle()` method and the now-unused `github.com/charmbracelet/lipgloss` import. Highlight rendering in `list.go` View() uses `th.SearchHighlight` directly via `RenderCompactRowWithBg`, so the helper was unreachable. Closes F-107 (P2 cleanup). |
| T-150 | DONE | HUMAN sign-off via tui-mcp — entry-list side of R13 AC 7 (`f` clears active list search): across 140x35 right + 80x24 right + 80x24 below on small.log, `/Appender` + Enter + `f` transferred focus to filter panel AND cleared orange SearchHighlight bg from match rows; keyhint row confirmed `focus: filter`. Streaming AppendEntries scenario (R13 streaming AC) visually deferred — tail-mode layout bug out of Tier 17 scope prevented visual replay; unit-test coverage pins the contract (AppendEntries_ExtendsActiveSearchMatches / ClearsNotFound_WhenFirstStreamedMatchArrives / InactiveSearch_NoOp). Tracked primary in impl-app-shell.md T-150. |
| T-143 | DONE | `search.go` — `SearchModel` (query, matches, current, inputMode, notFound). API: Activate/Deactivate/CommitInput/AppendRune/BackspaceRune (UTF-8-safe via `utf8.DecodeLastRuneInString`)/Next/Prev/MatchLines/CurrentMatchLine/CurrentIndex/NotFound. `composeSearchRow` mirrors `RenderCompactRow` field order (time|level|logger|msg), plain-text so matches align with rendered view. `list.go` integration: `search` field on `ListModel`; `Search()/ActivateSearch/DeactivateSearch/HasActiveSearch` accessors; `handleSearchKey` at top of Update consumes Esc/Enter/Backspace/runes (input mode) and n/N (navigate mode — moves cursor to match via `followCursor` honouring R12 scrolloff). View() paints `SearchHighlight` bg on non-cursor match rows via `RenderCompactRowWithBg`; cursor-row `CursorHighlight` bg takes priority (R13 AC 10). `SetFilter` auto-deactivates search. 10 tests in `search_test.go` (match scan, NotFound, Next/Prev wrap, UTF-8 backspace, Deactivate clears, view highlight rendering, cursor-row priority, SetFilter clear, scrolloff honoured on Next, visible-set unchanged). Closes F-101 (cavekit-entry-list R13). |
| T-151 | DONE | `entrylist/search.go` — extracted `matchRow(lowerNeedle, entry, width, cfg)` helper called from both `ExtendMatches` (streaming append) and `computeMatches` (full scan). Single source of truth for case-fold + substring semantics; needle is pre-lowered by callers to avoid re-lowercasing per entry. Existing 94 entrylist tests pass unchanged — refactor is purely internal. Closes F-116 (P2 future-drift risk). |
| T-154 | DONE | Test-pin R13 AC 7 mouse-off-list branch. New `TestModel_MouseClick_DetailZone_ClearsActiveListSearch` in `internal/ui/app/model_test.go`: open pane, activate list search + type `t`, send `tea.MouseMsg{Button: MouseLeft, Action: Press}` at `ListContentWidth()+4` (inside detail zone), assert focus = FocusDetailPane AND `!HasActiveSearch()` AND `Query() == ""`. Contract was MET in code (`app/model.go:524` — `DeactivateSearch` fires on zone transition) but only the focus-change half was pinned by TestModel_Click_DetailZone_TransfersFocusToDetail; this locks the paired search-clear. Closes F-119 (P3 test-pin gap). |
| T-163 | DONE | `entrylist/list.go`: added `contentTopYSet bool` field on `ListModel`, set to `true` only inside `WithContentTopY(y)`. `rowForY(y)` returns `-1` as its first guard when `!contentTopYSet`. Prevents silent regression of T-158's 2-row offset fix if the app-shell `WithContentTopY` wiring is ever dropped — without the guard, a fresh `ListModel` with `contentTopY == 0` would silently resolve terminal-Y=0 clicks to row 0. New `TestListModel_T163_RowForY_Rejects_When_ContentTopY_Unset` in `list_test.go`: fresh list + WindowSizeMsg (so ViewportHeight is normal — proving rejection is due to contentTopYSet, not a degenerate viewport) asserts `rowForY` returns -1 for y ∈ {0, 2, 5, 10, 15}; then after `WithContentTopY(2)` the resolver comes online (rowForY(2) = 0, rowForY(5) = 3). Full suite 550/550 across 11 packages (+1 new test). Closes F-127 (P3 regression vector). |
| T-158 | DONE | List side of single-owner click-row resolver (cavekit-entry-list R10 rewrite). `entrylist/list.go`: new `contentTopY int` field on `ListModel`; `WithContentTopY(y int)` setter called by app from `layout.ListContentTopY()`; `rowForY(y)` now subtracts `contentTopY` before checking against viewport and deriving the visible-index. Pairs with `appshell.Layout.ListContentTopY()` as the single owner — list no longer re-derives the header/border offset (that bug caused the historical 2-row click-offset). 2 new unit tests in `list_test.go`: `TestListModel_MouseClick_HonorsContentTopY_Offset2` (y=2 → Cursor=0; y=5 → Cursor=3 after offset) and `TestListModel_MouseClick_TopBorderBelowContentTopY_NoOp` (y=1 leaves cursor unchanged). Full suite 548 pass. |
| T-backprop-R14-signal | DONE | Backprop 2026-04-20 entry #8: tail-follow cursor snap (R14) must notify R10 selection-signal consumers so the detail pane re-renders in the same frame. `app/model.go` new `appendToList(entries)` helper: captures `prevCursor`, delegates to `list.AppendEntries`, and when `pane.IsOpen() && Cursor moved` calls `pane.Open(entry)` with current scrolloff + hiddenFields — same wiring the `entrylist.SelectionMsg` handler already runs for keyboard nav. Both `TailStreamMsg`/`TailMsg` and `LoadFileStreamMsg`/`EntryBatchMsg` branches swapped to the helper so neither append path can bypass the re-sync. 3 new regression tests in `app/model_test.go` (TailMsg resync, BatchMsg resync, NotAtTail defensive pin). 2 new exported test constructors in `logsource/tail.go` + `logsource/loader.go` (`NewTailStreamMsgForTest` + `NewLoadFileStreamMsgForTest`) let app-level tests drive the wrapper paths without real file I/O. Closes the R14 follow-up AC pair (selection signal on snap). See `.cavekit/history/backprop-log.md` #8. |
| T-backprop-R14 | DONE | Backprop 2026-04-20 entry #7: `entrylist/list.go` `AppendEntries` now advances Cursor + runs `followCursor` when pre-append Cursor was on the last entry; otherwise leaves cursor/offset untouched. New `IsAtTail()` accessor (Cursor == TotalEntries-1, false on empty). `app/model.go` View() wires `header.WithFollow(m.followMode && m.list.IsAtTail())` so the `[FOLLOW]` badge tracks cursor state live — lit at the tail in tail mode, dark once the user scrolls up, re-lit by `G`. 6 new regression tests: TestAppendEntries_TailFollow_AdvancesCursorAndViewport, TestAppendEntries_NotAtTail_NoFollow, TestAppendEntries_EmptyList_FirstAppend, TestIsAtTail_ReflectsCursorAtLast, TestIsAtTail_EmptyIsFalse, TestAppendEntries_PauseWithK_ResumeWithG. tui-mcp HUMAN sign-off on `/tmp/r14-live.jsonl` at 140x32 — startup FOLLOW lit at entry 40, external 5-line append → viewport 13-40 → 18-45 FOLLOW still on (no keypress); `k` → FOLLOW dark, cursor 44; 5 more lines → header 50/X but viewport frozen at 18-45; `G` → FOLLOW back, viewport 23-50; 3 more lines → viewport 26-53 FOLLOW still on. Closes `cavekit-entry-list.md` R14 all ACs. See `.cavekit/history/backprop-log.md` #7. |

## Revision Log
| Date | Commit | Issue | Cavekit Update | Plan Update |
|------|--------|-------|----------------|-------------|
| 2026-04-20 | 9e984ca, 14cd09a | Follow-mode viewport stays frozen when user is at the tail and new entries arrive | new R14 in cavekit-entry-list.md | T-backprop-R14 |
